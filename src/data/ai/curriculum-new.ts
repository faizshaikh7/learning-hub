import type { CurriculumTopic } from '@/types'

/**
 * Newly-added, current (2025-2026) AI topics kept separate from the base
 * curriculum so they can be woven into the correct learning sequence.
 */
export const AI_NEW_TOPICS: CurriculumTopic[] = [
  {
    id: 'beyond-rag',
    phase: 4,
    phaseName: 'RAG Systems',
    orderIndex: 99,
    estimatedMins: 35,
    prerequisites: ['rag-fundamentals', 'advanced-rag'],
    title: 'Beyond RAG: CAG, RIG & Managed Retrieval',
    eli5: "Classic RAG is like sending a librarian to fetch a few pages every time you ask a question. But now some models have huge memories, so instead you can hand them the WHOLE book once and just ask away (that's CAG). Google built another trick where the model, while answering, pauses to look up exact numbers from a trusted stats database and swaps out its guesses (that's RIG). And some tools now do all the fetching for you behind one button (managed retrieval).",
    analogy: "RAG = a research assistant who runs to the archive for each question. CAG = you photocopied the entire archive into the assistant's desk drawer, so no running needed. RIG = the assistant writes the essay but, whenever they state a statistic, they phone the official records office mid-sentence to replace their estimate with the verified figure. Managed retrieval (like Google File Search) = you drop your files in a box and a service files, indexes, and cites them automatically.",
    explanation: "\"Google replaced RAG\" is a headline, not the full story — RAG isn't dead, but the retrieval landscape has genuinely evolved. Here are the approaches now competing with and complementing classic retrieve-then-generate:\n\n1. CAG (Cache-Augmented Generation): instead of retrieving chunks at query time, you preload the entire knowledge source into the model's context window once, and reuse the model's KV cache across queries. Modern long-context models (e.g. Gemini's 1M–2M token windows) make this viable for small-to-medium corpora. No retriever, no vector DB, lower per-query latency once warm.\n\n2. RIG (Retrieval Interleaved Generation) — Google's approach: the model is fine-tuned to emit natural-language data queries (function-call style, e.g. `DC(\"population of India 2020\")`) DURING generation, which fetch ground-truth values from Google's Data Commons (a unified knowledge graph of public statistics from sources like the UN, CDC, and census bureaus). The model's guessed numbers get replaced with verified ones inline. RAG retrieves context UP FRONT; RIG interleaves retrieval WHILE generating.\n\n3. Managed retrieval (e.g. Google File Search in the Gemini API): a hosted service that ingests your files, chunks + embeds + indexes them automatically, and exposes a searchable, citation-aware corpus to your prompts — collapsing the whole DIY RAG pipeline into one managed capability.\n\n4. KAG (Knowledge-Augmented Generation): couples a knowledge graph with the LLM so it can reason over structured relationships (similar in spirit to GraphRAG) — stronger for multi-hop, logical questions than pure vector similarity.",
    technicalDeep: "CAG mechanics: load documents into the prompt, run one forward pass to build the KV cache, then answer many questions reusing that cache (prompt caching makes repeated queries cheap). Bounded by context length and the cost of very long prompts; there's no retrieval step to get wrong, but also no way to exceed the window. Best when the corpus is stable and fits (e.g. a product manual, a policy set).\n\nRIG mechanics: fine-tune the model to interleave `DC(question)`-style calls; each call resolves against Data Commons and substitutes a verified statistic for the model's estimate. This specifically targets numerical/statistical hallucination — the class of error long-context and vanilla RAG still get wrong when the right number simply isn't in the retrieved text. Tradeoff: it's scoped to what Data Commons covers, and requires the tuned model.\n\nManaged retrieval mechanics: you lose fine-grained control of chunking/embedding/reranking but gain speed-to-ship and built-in citations; good default for teams that don't want to run a vector DB.\n\nThe long-context debate: bigger windows reduce — but don't eliminate — the need for retrieval. For huge (billions of tokens), frequently-changing, or access-controlled corpora, retrieval still wins on cost, freshness, and security. In practice, production systems increasingly COMBINE these: retrieve a candidate set, stuff a large context (RAG+long-context), and let an agent call tools/APIs for exact facts (agentic RAG / RIG-style grounding).",
    whatBreaks: "Assuming CAG scales to any corpus: it's capped by the context window and gets expensive as prompts grow — it doesn't replace RAG for large or dynamic data. Assuming long context kills retrieval: for private, huge, or fast-changing knowledge, retrieval is still cheaper, fresher, and enforces access control. Using RIG for non-statistical grounding: RIG shines for numbers/stats in Data Commons — it's not a general document-QA replacement. Trusting managed retrieval blindly: you inherit its chunking/citation choices; verify recall and citation quality for YOUR data.",
    efficientWay: {
      title: 'Picking a grounding strategy',
      approaches: [
        { name: 'RAG (retrieve-then-generate)', verdict: 'best', reason: 'Default for large, dynamic, or access-controlled corpora needing citations and cost control. Still the workhorse.' },
        { name: 'CAG (preload + KV cache)', verdict: 'ok', reason: 'Great when the whole corpus is small, stable, and fits the context window — simplest pipeline, lowest latency.' },
        { name: 'RIG / tool-grounded facts', verdict: 'ok', reason: 'Best for verified numeric/statistical grounding; pair it with RAG rather than treating it as a full replacement.' },
        { name: 'Managed retrieval (File Search etc.)', verdict: 'ok', reason: 'Fastest path to a working, cited RAG when you don\'t want to run retrieval infra — at the cost of control.' },
      ],
      recommendation: "Match the tool to the corpus: small + stable → CAG; large/dynamic/private → RAG; numbers-must-be-exact → add RIG/tool grounding; ship-fast, don't-run-infra → managed retrieval. The strongest production systems blend them — retrieve, fill a long context, and call tools for exact facts — rather than betting on one.",
    },
    commonMistakes: [
      "Declaring 'RAG is dead' — the alternatives complement it for specific corpus shapes, they don't universally replace it",
      "Using CAG for a corpus that doesn't fit (or barely fits) the context window, blowing up cost and latency",
      "Expecting RIG to answer general document questions — it's built for verified statistics via Data Commons",
      "Adopting managed retrieval without checking recall/citation quality on your own data",
    ],
    seniorNotes: "The real 2025-2026 shift isn't 'X replaced RAG' — it's that grounding became a portfolio decision driven by corpus size, volatility, and the cost of being wrong. Long-context models made CAG viable and shrank naive RAG's advantage for small corpora; RIG/tool-use pushed factual grounding INTO the generation loop for exact values; managed services commoditized DIY pipelines. As an architect, decide per-use-case and instrument it: measure faithfulness/citation accuracy, freshness (staleness window), cost per answer, and latency — then let those numbers pick the strategy, and expect to combine several.",
    interviewQuestions: [
      "What is Cache-Augmented Generation and when does it beat RAG?",
      "How does Google's RIG differ from RAG in when and how it retrieves?",
      "Does a 2M-token context window make RAG obsolete? Argue both sides.",
      "What problem does RIG specifically target that vanilla RAG often gets wrong?",
      "When would you choose managed retrieval (e.g. Gemini File Search) over building your own RAG pipeline?",
    ],
    interviewAnswers: [
      "CAG preloads the whole knowledge source into the context window and reuses the KV cache instead of retrieving per query; it beats RAG when the corpus is small, stable, and fits the window — you get a simpler pipeline and lower per-query latency with no retriever to tune.",
      "RAG retrieves relevant context up front and prepends it before generation; RIG fine-tunes the model to emit data queries (function-call style) DURING generation that fetch verified values from Data Commons and replace the model's estimates inline.",
      "It doesn't: for large, frequently-changing, or access-controlled corpora, retrieval is cheaper, fresher, and enforces permissions, and huge prompts are costly and can dilute attention; but for small stable corpora, long context (CAG) is simpler and often better — so it's a per-corpus decision.",
      "Numerical/statistical accuracy — RIG replaces the model's guessed numbers with ground-truth values from Data Commons, addressing the case where the correct figure simply isn't present in retrieved text.",
      "When you want a working, citation-aware RAG quickly without operating a vector database and are comfortable delegating chunking/embedding/indexing choices to the provider — trading fine-grained control for speed-to-ship.",
    ],
    codeExamples: [
      {
        lang: 'python',
        label: 'CAG: preload the whole corpus, reuse via prompt caching',
        code: "# Cache-Augmented Generation: no retriever — the corpus lives in context.\n# Works when the corpus fits a long-context model (e.g. Gemini 1M-2M tokens).\nKNOWLEDGE = open('company_handbook.md').read()   # small, stable corpus\n\nsystem = f\"Answer ONLY from this handbook.\\n<handbook>\\n{KNOWLEDGE}\\n</handbook>\"\n\n# The handbook is sent once and cached; each question reuses the KV cache,\n# so repeated queries are fast and cheap (no vector DB, no retrieval step).\ndef ask(question: str) -> str:\n    return llm.generate(system=system, user=question, cache='handbook-v1')\n\nask('How many vacation days do new hires get?')\nask('What is the expense-approval limit for an L4?')",
      },
      {
        lang: 'text',
        label: 'RIG: retrieval interleaved DURING generation',
        code: "User: \"Compare the population growth of India and China since 2000.\"\n\nModel (drafts, but interleaves verified lookups instead of guessing):\n  \"India's population grew from DC(\"population India 2000\")\n   to DC(\"population India 2020\"), while China went from\n   DC(\"population China 2000\") to DC(\"population China 2020\")...\"\n\n# Each DC(...) call resolves against Google's Data Commons knowledge graph\n# and the verified number is substituted inline — so the final answer's\n# statistics are grounded, not hallucinated.\n#\n# RAG:  retrieve context  -> then generate.\n# RIG:  generate + call for exact facts  -> interleaved.",
      },
    ],
  },
  {
    id: 'ai-ml-dl-foundations',
    phase: 0,
    phaseName: 'AI & LLM Foundations',
    orderIndex: 99,
    estimatedMins: 25,
    prerequisites: ['ai-engineer-intro'],
    title: 'AI vs ML vs Deep Learning: The Hierarchy',
    eli5: "These words nest inside each other like Russian dolls. AI is the biggest doll — anything that mimics human smarts. Inside it is Machine Learning — programs that learn from examples instead of being told every rule. Inside that is Deep Learning — learning with brain-like neural networks. And the AI you chat with (LLMs) lives in the smallest doll.",
    analogy: "Think of nesting dolls: AI ⊃ Machine Learning ⊃ Deep Learning ⊃ Transformers/LLMs. A rule-based chess engine is AI but not ML. A spam filter that learns from examples is ML. An image recognizer built on neural nets is Deep Learning. ChatGPT is a deep-learning transformer.",
    explanation: "Getting this hierarchy right prevents endless confusion. AI is any system that mimics human intelligence — including old-school hard-coded rules (a chess program, a thermostat's logic). Machine Learning is the subset that learns patterns from data instead of following hand-written rules. Deep Learning is the subset of ML that uses neural networks — layers of weighted connections loosely inspired by neurons. LLMs are deep-learning models (specifically transformers) trained on massive text to predict the next token.\n\nThe other idea that made modern AI explode is transfer learning: instead of training a model from scratch (millions of dollars, Google-scale data), you start from a pretrained foundation model and adapt it. That's why a small team can build sophisticated AI today — you're standing on a pretrained giant and specializing it with prompting, RAG, or fine-tuning.",
    technicalDeep: "A neural network is layers of nodes; each connection has a weight, training adjusts those weights via backpropagation to minimize error. 'Deep' just means many layers. Architecture families matter: Transformers (self-attention, parallel) dominate text and increasingly everything (GPT, Claude, Gemini); CNNs (convolutions) excel at images; RNNs/LSTMs handled sequences before transformers and are now largely legacy for language. As an AI engineer you almost never design or train these from scratch — you consume pretrained foundation models via APIs or open weights and adapt them. The progression of adaptation, cheapest to most expensive: prompting → RAG → fine-tuning → training from scratch.",
    whatBreaks: "Conflating the terms leads to bad decisions: calling every automation 'AI/ML', or assuming you must 'train a model' when a prompt or RAG would do. Picking the wrong architecture (e.g. reaching for an LLM to classify images) wastes time and money. Believing you need Google-scale data/compute stops small teams from shipping — transfer learning is exactly what removes that barrier.",
    efficientWay: {
      title: 'Learning the hierarchy without drowning in theory',
      approaches: [
        { name: 'Learn the nesting + transfer learning conceptually', verdict: 'best', reason: 'You need the mental map to choose tools and talk credibly — not to implement backprop.' },
        { name: 'Dive deep into CNN/RNN math first', verdict: 'weak', reason: 'For AI-engineering on foundation models this is rarely used day to day; learn it later if you go into ML research.' },
        { name: 'Skip fundamentals, jump to building', verdict: 'ok', reason: 'You can ship, but you’ll misdiagnose problems (‘fine-tune it!’ when a prompt fixes it) without the map.' },
      ],
      recommendation: "Internalize AI ⊃ ML ⊃ DL ⊃ Transformers, and the prompting → RAG → fine-tuning → from-scratch ladder. That mental model lets you pick the cheapest adaptation that solves the problem — which is 90% of the AI-engineering job.",
    },
    commonMistakes: [
      "Using 'AI' and 'ML' interchangeably — plenty of AI (rule systems) isn't ML, and not all ML is deep learning",
      "Assuming you must train a model from scratch when prompting or RAG would solve it far cheaper",
      "Reaching for an LLM for tasks a CNN or classic ML model does better (and cheaper), like image classification",
      "Thinking 'deep' means smarter — it just means more layers; architecture fit matters more than depth",
    ],
    seniorNotes: "AI engineers operate at the application layer on top of foundation models. You need this hierarchy to reason about tradeoffs and communicate, but the leverage is in adaptation (prompt/RAG/fine-tune), not in re-deriving transformers. When someone proposes 'let's train our own model,' the senior move is usually to climb down the ladder: can prompting, RAG, or a small fine-tune get us 90% for 1% of the cost?",
    interviewQuestions: [
      "Explain the relationship between AI, machine learning, and deep learning.",
      "What is transfer learning and why did it make modern AI accessible to small teams?",
      "When would you NOT use an LLM, and what would you use instead?",
      "Walk me through the cheapest-to-most-expensive ways to adapt a model to a task.",
    ],
    interviewAnswers: [
      "AI is any system mimicking human intelligence (including rule-based); machine learning is the subset that learns from data instead of hard-coded rules; deep learning is the subset of ML using multi-layer neural networks; LLMs are deep-learning transformers for text.",
      "Transfer learning starts from a pretrained model and adapts it instead of training from scratch, so you inherit billions of dollars of pretraining and specialize with a little data — that's why small teams can build capable AI without Google-scale resources.",
      "For image classification, structured tabular prediction, or ultra-low-latency/edge tasks, a CNN or classic ML model is cheaper, faster, and more accurate than an LLM — use the architecture that fits the data type.",
      "Prompting (free, instant) → RAG (ground in your data) → fine-tuning (teach style/format on your examples) → training from scratch (nuclear option); climb only as high as the problem forces you.",
    ],
    codeExamples: [
      {
        lang: 'text',
        label: 'The nesting hierarchy + the adaptation ladder',
        code: "AI  (mimics intelligence — includes rule-based systems)\n └─ Machine Learning  (learns patterns from data)\n     └─ Deep Learning  (neural networks with many layers)\n         └─ Transformers  →  LLMs (GPT, Claude, Gemini)\n\nAdapt a model — cheapest to most expensive:\n  1. Prompting        (seconds, free)      ← start here\n  2. RAG              (ground in your docs)\n  3. Fine-tuning      (teach style/format on your data)\n  4. Train from scratch (millions $ — rarely needed)",
      },
    ],
  },
  {
    id: 'transformers-attention',
    phase: 0,
    phaseName: 'AI & LLM Foundations',
    orderIndex: 99,
    estimatedMins: 30,
    prerequisites: ['llm-fundamentals'],
    title: 'Transformers & the Attention Mechanism',
    eli5: "When you read 'the bank was steep,' you instantly know it's a riverbank, not money — because of the word 'steep.' Attention is how AI does the same thing: for every word, it looks at the surrounding words and decides which ones matter to understand it. Give it clear context and it decides well; starve it of context and it guesses.",
    analogy: "Attention is a spotlight. For each word the model is processing, it shines the spotlight on the other words that help explain it — brightly on the relevant ones, dimly on the rest. 'Steep' lights up when interpreting 'bank'; 'deposit' would light up in a finance sentence.",
    explanation: "Transformers are the architecture behind every modern LLM, and their superpower is self-attention. Instead of reading left-to-right one word at a time (like older RNNs), a transformer looks at all tokens at once and, for each token, computes how much every other token should influence its meaning. That weighting is 'attention.'\n\nThis single idea explains a huge amount of prompt behavior. Clear, relevant context gives the model strong signals to attend to, so it makes better decisions. Vague or missing context leaves it guessing. And piling in huge amounts of irrelevant text hurts, because the truly relevant tokens now compete for the spotlight — this is why precise, well-structured prompts and good retrieval beat 'dump everything in.'",
    technicalDeep: "Each token is projected into three vectors: Query, Key, and Value. Attention score between two tokens ≈ softmax(Q·Kᵀ / √d), then you take a weighted sum of Values — so each token's new representation is a context-aware blend of the tokens it attends to. Multi-head attention runs several of these in parallel to capture different relationships; positional encodings inject word order (since everything is processed in parallel). Stacking many such layers builds deep contextual understanding.\n\nWhy transformers beat RNNs: parallelism (train efficiently on GPUs) and no vanishing-gradient over long sequences (better long-range dependencies). The catch: self-attention is O(n²) in sequence length — doubling context roughly quadruples compute — which is exactly why long context windows are expensive and why the KV cache exists to avoid recomputing attention over the prompt for every new token.",
    whatBreaks: "Ambiguous prompts starve attention of the context it needs to disambiguate — you get plausible-but-wrong interpretations. Over-long, noisy context causes the 'lost in the middle' effect: relevant tokens get diluted and the model attends to the wrong things. Ignoring structure (headings, XML tags, ordering) makes it harder for attention to find what matters. And underestimating the O(n²) cost leads to surprise latency/bills at long context.",
    efficientWay: {
      title: 'Using attention to your advantage in prompts',
      approaches: [
        { name: 'Give clear, relevant, well-structured context', verdict: 'best', reason: 'Strong signals for attention → the model disambiguates correctly and stays on task.' },
        { name: 'Dump everything and hope', verdict: 'weak', reason: 'Relevant tokens compete with noise; quality drops and cost rises with the O(n²) attention.' },
        { name: 'Ultra-terse prompts with no context', verdict: 'ok', reason: 'Fine for simple asks, but the model guesses on anything ambiguous.' },
      ],
      recommendation: "Feed the model the right context, structured and ordered, rather than the most context. Understanding attention is why context engineering, XML structure for Claude, and precise RAG retrieval all work — they make the relevant tokens easy to attend to.",
    },
    commonMistakes: [
      "Assuming more context always helps — beyond a point it dilutes attention and adds O(n²) cost",
      "Writing ambiguous prompts and blaming the model when it disambiguates 'wrong'",
      "Ignoring structure/ordering — burying the key instruction in the middle of a wall of text",
      "Forgetting that context length drives latency and cost quadratically, not linearly",
    ],
    seniorNotes: "Attention is the mental model that explains most prompt phenomena: why structure and XML tags help Claude, why 'lost in the middle' happens, why RAG precision matters more than recall past a point, and why the KV cache / prompt caching save money. When a prompt misbehaves, ask 'what is the model being asked to attend to, and is the relevant context clear and close?' — that question resolves most issues.",
    interviewQuestions: [
      "In one paragraph, what does the attention mechanism do?",
      "Why did transformers replace RNNs for language tasks?",
      "Why does self-attention make very long context windows expensive?",
      "Practically, how does understanding attention change how you write prompts?",
    ],
    interviewAnswers: [
      "For each token, self-attention computes how much every other token should influence its representation (via Query·Key scores), then blends their Values accordingly — producing context-aware meaning, e.g. resolving 'bank' using nearby words.",
      "Transformers process all tokens in parallel (fast on GPUs) and capture long-range dependencies without the vanishing gradients that plagued sequential RNNs, so they train better and understand longer context.",
      "Attention compares every token to every other token, so cost grows O(n²) with sequence length — doubling context roughly quadruples the attention compute, which is why long windows are costly and the KV cache is used to avoid recomputation.",
      "You provide clear, relevant, well-structured context and put key instructions where they're easy to attend to, rather than dumping everything in — because attention quality, not context quantity, drives output quality.",
    ],
    codeExamples: [
      {
        lang: 'text',
        label: "Attention resolving ambiguity ('bank')",
        code: "Sentence: \"the river bank was steep\"\n\nProcessing the word 'bank', attention weighs the others:\n  the    ▁ (low)\n  river  █████ (high)  ← pushes 'bank' toward riverbank\n  was    ▁ (low)\n  steep  ████  (high)  ← reinforces geography, not finance\n\nSame word, finance context: \"deposit cash at the bank\"\n  deposit █████ , cash ████  → 'bank' = financial institution\n\nLesson: the surrounding context you provide *is* what the\nmodel attends to. Clear context → correct interpretation.",
      },
    ],
  },
  {
    id: 'rlhf-alignment',
    phase: 0,
    phaseName: 'AI & LLM Foundations',
    orderIndex: 99,
    estimatedMins: 25,
    prerequisites: ['transformers-attention'],
    title: 'RLHF & How Models Become Helpful',
    eli5: "A freshly-trained model is just a super-powered autocomplete — it predicts likely next words, but it won't necessarily answer your question helpfully. RLHF is the training step that taught it to be a helpful assistant: humans rated lots of answers as good or bad, and the model learned to produce the good ones.",
    analogy: "Pretraining produces a brilliant but unfiltered intern who has read the whole internet. RLHF is the mentorship: show them thousands of examples of good vs bad responses, reward the good, and they turn into a helpful, polite, mostly-safe assistant.",
    explanation: "Modern chat models are built in stages. (1) Pretraining: predict the next token over trillions of words — the model learns language and facts but only knows how to continue text, not to be helpful. (2) Supervised fine-tuning / instruction tuning: train on curated instruction→response examples so it learns to follow instructions. (3) RLHF (Reinforcement Learning from Human Feedback): humans rank multiple candidate responses, a 'reward model' is trained to predict human preference, and the LLM is then optimized to maximize that reward.\n\nThis is why ChatGPT-style models feel helpful, follow instructions, and refuse harmful requests — behaviors a raw pretrained model does not reliably have. It's also the source of some quirks: sycophancy (agreeing to please), over-refusal of benign requests, and verbosity, because those can score well with raters.",
    technicalDeep: "The RLHF loop: collect preference data (humans pick better of two responses) → train a reward model on those preferences → fine-tune the LLM with reinforcement learning (classically PPO) to maximize reward, with a KL penalty that keeps it from drifting too far from the supervised model (preventing it from 'gaming' the reward into gibberish). Newer approaches simplify this: DPO (Direct Preference Optimization) trains directly on preference pairs without a separate reward model or RL loop, and RLAIF / Constitutional AI use AI-generated feedback guided by a set of principles to reduce reliance on human labels. Crucially, RLHF optimizes for *seeming* helpful and preferred — not for being correct — which is part of why hallucination persists.",
    whatBreaks: "Over-optimizing the reward causes reward hacking: sycophancy, hedging, refusing safe requests, or padding answers because raters liked longer/agreeable responses. There's also an 'alignment tax' — alignment can slightly reduce raw capability. And because RLHF rewards perceived helpfulness, a confident wrong answer can score well, so alignment does not eliminate hallucination — you still need grounding (RAG) and verification.",
    efficientWay: {
      title: 'What an AI engineer actually needs from RLHF',
      approaches: [
        { name: 'Understand it conceptually to explain model behavior', verdict: 'best', reason: 'RLHF explains sycophancy, refusals, verbosity, and why models seem helpful — you consume aligned models, you don’t run PPO.' },
        { name: 'Learn DPO/RLHF to fine-tune preferences yourself', verdict: 'ok', reason: 'Useful only when you have preference data and a real need to shape behavior beyond prompting.' },
        { name: 'Ignore it and treat model quirks as random', verdict: 'weak', reason: 'You’ll misread sycophancy/refusals as bugs and prompt around them blindly.' },
      ],
      recommendation: "Know the three stages (pretrain → SFT → RLHF/DPO) and that RLHF optimizes for preferred, not true. That lets you explain model behavior, pick better models, and remember to add grounding + verification for correctness.",
    },
    commonMistakes: [
      "Believing an aligned model is therefore truthful — RLHF rewards helpful-sounding answers, not correct ones",
      "Reading every refusal as 'the model can't' rather than 'it was trained not to'",
      "Attributing sycophancy or verbosity to randomness instead of reward optimization",
      "Assuming alignment removes the need for RAG/verification on factual tasks",
    ],
    seniorNotes: "RLHF is the single best explanation for the 'personality' of modern models — helpfulness, refusals, sycophancy, hedging. Anthropic's Constitutional AI and the shift to DPO are the current directions worth knowing. When comparing models, a lot of what you perceive as 'tone' or 'follows instructions well' is really differences in their alignment training, not raw capability.",
    interviewQuestions: [
      "What are the three training stages of a modern chat LLM?",
      "What problem does RLHF solve that pretraining alone does not?",
      "Why doesn't RLHF eliminate hallucination?",
      "What is DPO and how does it differ from classic RLHF?",
    ],
    interviewAnswers: [
      "Pretraining (predict next token on massive text), supervised/instruction fine-tuning (follow instructions from example pairs), and RLHF (optimize toward human-preferred responses via a reward model).",
      "Pretraining yields a capable text-continuer that isn't reliably helpful or safe; RLHF aligns it to follow instructions and refuse harmful requests by optimizing for human-preferred behavior.",
      "Because RLHF rewards responses humans *prefer*, and a confident, well-written wrong answer can be preferred — it optimizes for perceived helpfulness, not truth, so you still need grounding and verification.",
      "DPO (Direct Preference Optimization) fine-tunes directly on preference pairs without training a separate reward model or running an RL loop, making alignment simpler and more stable than classic PPO-based RLHF.",
    ],
    codeExamples: [
      {
        lang: 'text',
        label: 'How a raw model becomes a helpful assistant',
        code: "1) PRETRAIN   — predict next token on ~trillions of words\n              result: knows language + facts, but only 'continues' text\n\n2) SFT        — fine-tune on instruction → response examples\n              result: follows instructions\n\n3) RLHF/DPO   — humans rank responses → reward model → optimize LLM\n              result: helpful, harmless, refuses bad asks\n\nKey caveat: steps 2–3 optimize for PREFERRED answers,\nnot TRUE ones — so hallucination survives. Ground with RAG\nand verify facts.",
      },
    ],
  },
]
