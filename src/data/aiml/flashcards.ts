import type { FlashcardData } from '@/types'

/** AI/ML In-Depth flashcard deck keyed by topic id. */
export const AIML_FLASHCARDS: FlashcardData = {
  'descriptive-statistics': [
    { id: 'mlfc_desc_1', q: 'When should you report the median instead of the mean?', a: 'When the distribution is skewed or has outliers — income, house prices, response times. The mean gets dragged toward extreme values while the median stays at the true "typical" point. If mean and median differ a lot, that gap itself signals skew.', hint: 'One billionaire walks into a bar...' },
    { id: 'mlfc_desc_2', q: 'What does standard deviation actually tell you, in plain terms?', a: 'The typical distance of a data point from the mean, in the same units as the data. For roughly normal data, about 68 percent of points fall within 1 sd of the mean, 95 percent within 2, and 99.7 percent within 3.' },
    { id: 'mlfc_desc_3', q: 'Why divide by n-1 instead of n when computing sample variance?', a: 'Bessel\'s correction. The sample mean is computed from the same data, so deviations from it are systematically smaller than deviations from the true population mean. Dividing by n-1 inflates the estimate just enough to make it unbiased.' },
    { id: 'mlfc_desc_4', q: 'What are quartiles and the IQR, and what are they used for?', a: 'Quartiles split sorted data into four equal parts: Q1 (25th percentile), Q2 (median), Q3 (75th percentile). IQR = Q3 - Q1, the span of the middle 50 percent. A common outlier rule flags points beyond Q1 - 1.5*IQR or Q3 + 1.5*IQR — this is what box plots draw.' },
    { id: 'mlfc_desc_5', q: 'What is skewness and how do you spot it from mean vs median?', a: 'Skewness measures asymmetry. Right (positive) skew has a long tail of high values, so mean > median (e.g. income). Left skew has a long low tail, so mean < median. Symmetric data has mean ≈ median.' },
  ],
  'variance-covariance': [
    { id: 'mlfc_varcov_1', q: 'What is covariance and what does its sign mean?', a: 'Covariance measures whether two variables move together: cov(X,Y) = average of (x - mean_x)(y - mean_y). Positive = they rise together, negative = one rises as the other falls, near zero = no linear co-movement. Its magnitude depends on units, so it is hard to compare across pairs.' },
    { id: 'mlfc_varcov_2', q: 'How does correlation fix the weakness of covariance?', a: 'Correlation divides covariance by both standard deviations: r = cov(X,Y) / (sd_X * sd_Y). This strips out the units, forcing the value into [-1, 1] so relationships are comparable across variable pairs. r = 0.8 means the same strength whether measuring heights or dollars.' },
    { id: 'mlfc_varcov_3', q: 'Does correlation of zero mean the variables are independent?', a: 'No. Correlation only detects LINEAR relationships. Y = X^2 on symmetric data has correlation 0 despite perfect dependence. Independence implies zero correlation, but not the reverse. Always plot the data.' },
    { id: 'mlfc_varcov_4', q: 'What is a covariance matrix and why does ML care about it?', a: 'For d features, a d x d symmetric matrix with variances on the diagonal and pairwise covariances off it. It captures the full linear structure of a dataset. PCA is literally the eigendecomposition of this matrix, and Gaussian models are parameterized by it.' },
    { id: 'mlfc_varcov_5', q: 'What happens to variance when you scale a variable by a constant a?', a: 'Var(aX) = a^2 * Var(X) — variance scales by the SQUARE of the constant, because it is measured in squared units. Standard deviation scales linearly: sd(aX) = |a| * sd(X). This is why feature scaling changes covariance structure and matters before PCA and distance-based methods.' },
  ],
  'bayes-theorem': [
    { id: 'mlfc_bayes_1', q: 'State Bayes theorem and name each part.', a: 'P(H|D) = P(D|H) * P(H) / P(D). Posterior (belief in hypothesis after data) = likelihood (probability of the data if the hypothesis is true) times prior (belief before data), normalized by the evidence P(D).', hint: 'Posterior = likelihood x prior / evidence' },
    { id: 'mlfc_bayes_2', q: 'A disease affects 1 in 1000 people. A test is 99 percent accurate. You test positive — roughly what is the chance you are sick?', a: 'Only about 9 percent. Out of 1000 people, ~1 true positive but ~10 false positives (1 percent of 999), so a positive result is 1 in 11 real. The tiny prior (base rate) dominates the accurate test. Ignoring this is the base-rate fallacy.' },
    { id: 'mlfc_bayes_3', q: 'What is the difference between P(A|B) and P(B|A), and why does it matter?', a: 'They answer opposite questions and can differ wildly: P(positive test | sick) may be 0.99 while P(sick | positive test) is 0.09. Confusing them is the prosecutor\'s fallacy. Bayes theorem is exactly the machine for converting one into the other using the prior.' },
    { id: 'mlfc_bayes_4', q: 'How does Naive Bayes classification use Bayes theorem, and what is "naive" about it?', a: 'It picks the class maximizing P(class) * product of P(feature_i | class). The naive part is assuming features are conditionally independent given the class — usually false, yet the classifier still works well for text (spam filtering) because it only needs the right argmax, not correct probabilities.' },
    { id: 'mlfc_bayes_5', q: 'In Bayesian terms, what is the difference between the prior and the posterior?', a: 'The prior encodes belief before seeing data; the posterior is the updated belief after multiplying in the likelihood of the observed data. Today\'s posterior becomes tomorrow\'s prior — Bayesian updating is iterative learning from evidence.' },
  ],
  'maximum-likelihood': [
    { id: 'mlfc_mle_1', q: 'What question does maximum likelihood estimation (MLE) answer?', a: 'Given observed data and a model family with unknown parameters, which parameter values make the observed data most probable? MLE picks theta maximizing L(theta) = P(data | theta). It is the workhorse principle for fitting almost every classical ML model.' },
    { id: 'mlfc_mle_2', q: 'Why do we maximize log-likelihood instead of likelihood?', a: 'Likelihood is a product of many small probabilities, which underflows numerically and is awkward to differentiate. Log turns the product into a sum, is monotonic (same argmax), and yields clean gradients. By convention we minimize NEGATIVE log-likelihood, matching the "minimize loss" framing.' },
    { id: 'mlfc_mle_3', q: 'How does MLE with Gaussian noise lead to mean squared error?', a: 'Assume y = f(x) + Gaussian noise. The log-likelihood of the data then contains -(y - f(x))^2 terms, so maximizing likelihood is exactly minimizing the sum of squared errors. MSE is not arbitrary — it IS Gaussian MLE.', hint: 'MSE = Gaussian assumption in disguise' },
    { id: 'mlfc_mle_4', q: 'What loss does MLE produce for classification, and from what assumption?', a: 'Cross-entropy (log loss). Model the label as a Bernoulli/categorical variable with probability given by the model; the negative log-likelihood of the labels is exactly cross-entropy. Every time you train with cross-entropy you are doing MLE on a categorical model.' },
    { id: 'mlfc_mle_5', q: 'How does adding a prior change MLE, and what is the ML name for the result?', a: 'Maximizing posterior instead of likelihood (MAP estimation) adds log P(theta) to the objective. A Gaussian prior on weights yields exactly L2 regularization; a Laplace prior yields L1. Regularization is a prior belief that weights should be small.' },
  ],
  'eigenvalues-eigenvectors': [
    { id: 'mlfc_eigen_1', q: 'What is an eigenvector, geometrically?', a: 'A direction that a matrix transformation does not rotate — vectors along it only get stretched or flipped. Av = lambda*v: apply the matrix, and the vector stays on its own line, scaled by the eigenvalue lambda.' },
    { id: 'mlfc_eigen_2', q: 'What does the eigenvalue tell you about its eigenvector\'s direction?', a: 'The scale factor along that direction: |lambda| > 1 stretches, |lambda| < 1 shrinks, negative flips orientation, and lambda = 0 collapses that direction entirely (the matrix is singular). The largest eigenvalue marks the direction the transformation amplifies most.' },
    { id: 'mlfc_eigen_3', q: 'How do eigenvectors enable PCA?', a: 'PCA eigendecomposes the data\'s covariance matrix. Its eigenvectors are the principal components — orthogonal directions of maximum variance — and each eigenvalue is the variance captured along that direction. Keeping the top-k eigenvectors gives the best k-dimensional linear compression.' },
    { id: 'mlfc_eigen_4', q: 'Why are covariance-matrix eigenvalues always real and non-negative, and why is that convenient?', a: 'Covariance matrices are symmetric positive semi-definite: symmetry guarantees real eigenvalues and orthogonal eigenvectors; PSD guarantees eigenvalues >= 0 (variance cannot be negative). So the eigenvalues can be safely ranked and read as variance explained.' },
    { id: 'mlfc_eigen_5', q: 'Beyond PCA, where else do eigen-ideas appear in ML?', a: 'Spectral clustering (eigenvectors of graph Laplacians), PageRank (dominant eigenvector of the link matrix), the Hessian\'s eigenvalues describing loss-surface curvature, and power iteration for fast top-eigenvector computation on huge matrices.' },
  ],
  'gradient-descent': [
    { id: 'mlfc_gd_1', q: 'Explain the gradient descent update rule and each term.', a: 'theta = theta - lr * gradient of loss w.r.t. theta. The gradient points in the direction of steepest INCREASE of loss, so stepping against it decreases loss. The learning rate lr controls step size. Repeat until convergence.', hint: 'Walk downhill in the fog, one step at a time' },
    { id: 'mlfc_gd_2', q: 'What goes wrong when the learning rate is too high or too low?', a: 'Too high: steps overshoot the minimum, loss oscillates or diverges to NaN. Too low: training crawls and can stall on plateaus. The practical fix is a schedule — start higher, decay over time — or an adaptive optimizer like Adam.' },
    { id: 'mlfc_gd_3', q: 'Batch vs stochastic vs mini-batch gradient descent — what is the tradeoff?', a: 'Batch uses all data per step: exact gradient, expensive, can sit in sharp minima. Stochastic (single example): cheap, noisy, erratic. Mini-batch (32-512 examples) is the standard compromise: GPU-efficient, and the gradient noise even helps escape saddle points and poor minima.' },
    { id: 'mlfc_gd_4', q: 'What do momentum and Adam add on top of vanilla gradient descent?', a: 'Momentum accumulates an exponential moving average of gradients — dampens zigzag in ravines and pushes through small bumps. Adam additionally keeps a per-parameter average of squared gradients and divides by it, giving each weight its own adaptive step size. Adam is the robust default.' },
    { id: 'mlfc_gd_5', q: 'Why does gradient descent work reliably on convex losses but is trickier for neural networks?', a: 'A convex loss (linear/logistic regression) has one global minimum — descent with a sane learning rate will find it. Neural losses are non-convex with saddle points, plateaus, and many local minima. In practice high dimensionality, mini-batch noise, and good initialization make wide, well-generalizing minima reachable anyway.' },
  ],
  'train-test-cross-validation': [
    { id: 'mlfc_cv_1', q: 'Why must you hold out a test set at all?', a: 'Training error is a biased, optimistic estimate — a model can memorize noise and score perfectly on data it saw. Only performance on unseen data estimates generalization, which is the actual goal. The test set simulates the future data the model will face.' },
    { id: 'mlfc_cv_2', q: 'What is k-fold cross-validation and why prefer it over a single validation split?', a: 'Split data into k folds; train on k-1, validate on the held-out fold, rotate k times, average the scores. Every point serves in validation exactly once, giving a lower-variance estimate than one arbitrary split — especially valuable on small datasets. k = 5 or 10 is standard.' },
    { id: 'mlfc_cv_3', q: 'What is data leakage and what is the most common way it sneaks in?', a: 'Information from validation/test data influencing training, producing scores that collapse in production. Classic culprit: fitting a scaler or imputer on the FULL dataset before splitting. Fix: fit all preprocessing inside the training fold only — which is what sklearn Pipelines inside cross_val_score guarantee.' },
    { id: 'mlfc_cv_4', q: 'Why do you need a validation set separate from the test set?', a: 'Hyperparameter tuning selects whatever looks best on the data used for selection, overfitting to it. Tune on validation (or CV folds), then measure the final chosen model ONCE on the untouched test set. If you iterate against the test set, it silently becomes a second validation set.' },
    { id: 'mlfc_cv_5', q: 'How does splitting change for time-series or imbalanced data?', a: 'Time series: never shuffle — train on the past, validate on the future (expanding/rolling window, sklearn TimeSeriesSplit), else you leak the future. Imbalanced classes: use stratified splits so every fold preserves class proportions, keeping metrics stable and rare classes present in each fold.' },
  ],
  'model-evaluation': [
    { id: 'mlfc_eval_1', q: 'Why is accuracy misleading on imbalanced data, and what should you look at instead?', a: 'With 99 percent negatives, always predicting "negative" scores 99 percent accuracy while catching nothing. Look at the confusion matrix and per-class metrics: precision, recall, F1, and PR-AUC — they expose performance on the minority class that accuracy hides.' },
    { id: 'mlfc_eval_2', q: 'Define precision and recall in terms of the confusion matrix.', a: 'Precision = TP / (TP + FP): of everything predicted positive, how much was right — the cost of false alarms. Recall = TP / (TP + FN): of everything actually positive, how much was found — the cost of misses. Raising the decision threshold typically trades recall for precision.', hint: 'Precision = trust the alarm; recall = catch them all' },
    { id: 'mlfc_eval_3', q: 'What does ROC-AUC = 0.9 actually mean?', a: 'Take a random positive and a random negative: 90 percent of the time the model scores the positive higher. It measures ranking quality across ALL thresholds, making it good for comparing models before choosing an operating point. Under heavy class imbalance prefer PR-AUC, which is stricter about false positives.' },
    { id: 'mlfc_eval_4', q: 'Compare MAE and RMSE — when does each fit?', a: 'Both measure average regression error in original units. RMSE squares errors first, so large misses dominate — use it when big errors are disproportionately bad. MAE weighs all errors linearly and is robust to outliers — use it when a few extreme points should not drive the score. RMSE >= MAE always; a big gap signals outliers.' },
    { id: 'mlfc_eval_5', q: 'What does R-squared measure and what is a common misreading of it?', a: 'The fraction of target variance the model explains versus just predicting the mean: 1 is perfect, 0 ties the mean baseline, negative is worse than the mean. Misreadings: high R-squared does not mean causation or good predictions on new data (it can be inflated by overfitting), and adding any feature never lowers in-sample R-squared — use adjusted R-squared or held-out data.' },
  ],
  'bias-variance-tradeoff': [
    { id: 'mlfc_bv_1', q: 'Decompose a model\'s expected error into its three parts.', a: 'Expected error = bias^2 + variance + irreducible noise. Bias: systematic error from a too-simple model. Variance: sensitivity to which training sample you happened to draw. Noise: inherent randomness no model can remove. You control the first two; the third is the floor.' },
    { id: 'mlfc_bv_2', q: 'How do you diagnose high bias vs high variance from train and validation errors?', a: 'High bias (underfitting): training error is already high, validation similar — the model cannot even fit what it sees. High variance (overfitting): training error low but validation much worse — the model memorized noise. The train-validation GAP is the variance signal; the train error LEVEL is the bias signal.', hint: 'Gap = variance, level = bias' },
    { id: 'mlfc_bv_3', q: 'Your model has high variance. Name four remedies.', a: 'Get more training data; add regularization (L1/L2, dropout); reduce model complexity (shallower trees, fewer features); use early stopping; or ensemble by averaging (bagging / random forests). All shrink the model\'s ability to memorize sample noise.' },
    { id: 'mlfc_bv_4', q: 'Why does collecting more data NOT fix a high-bias model?', a: 'A high-bias model is too simple to represent the signal — a straight line through curved data stays a straight line no matter how many points you add. More data only reduces variance. For bias: increase capacity, add better features, or reduce regularization.' },
    { id: 'mlfc_bv_5', q: 'How do bagging and boosting map onto the bias-variance tradeoff?', a: 'Bagging (random forests) averages many independently overfit low-bias trees, cancelling their noise — it attacks VARIANCE. Boosting adds weak high-bias learners sequentially, each fitting remaining errors — it attacks BIAS (while shrinkage and early stopping keep variance in check). The two ensembles are mirror-image cures.' },
  ],
  'regularization': [
    { id: 'mlfc_reg_1', q: 'What is regularization and why does it improve test performance while hurting training fit?', a: 'A penalty on model complexity (typically weight size) added to the loss: loss + lambda * penalty. It constrains the model, deliberately trading a bit of bias for a large drop in variance, so the model captures signal instead of memorizing noise. Training error rises slightly; generalization improves.' },
    { id: 'mlfc_reg_2', q: 'Why does L1 produce exactly-zero weights while L2 only shrinks them?', a: 'Geometrically, L1\'s constraint region is a diamond with corners ON the axes — the loss surface usually touches a corner, where some coordinates are exactly zero. L2\'s region is a sphere with no corners, so the touchpoint has all coordinates small but nonzero. Also, L1\'s gradient is a constant push toward zero; L2\'s push vanishes as weights approach zero.', hint: 'Diamond has corners, circle does not' },
    { id: 'mlfc_reg_3', q: 'How do you choose the regularization strength lambda?', a: 'Cross-validation over a logarithmic grid (e.g. 0.001 to 100). Too small: overfitting remains. Too large: underfitting — the model is squashed toward zero. Watch the validation curve for the sweet spot. Note sklearn\'s LogisticRegression uses C = 1/lambda, so SMALLER C means STRONGER regularization.' },
    { id: 'mlfc_reg_4', q: 'What is the Bayesian interpretation of L1 and L2 regularization?', a: 'MAP estimation with a prior on weights: L2 corresponds to a Gaussian prior (weights probably small, smoothly distributed), L1 to a Laplace prior (sharp peak at zero — many weights probably exactly irrelevant). Regularization is a prior belief encoded in the objective.' },
    { id: 'mlfc_reg_5', q: 'Name three regularization techniques beyond L1/L2 penalties.', a: 'Dropout (randomly silence neurons during training so none becomes indispensable), early stopping (halt when validation loss turns upward), and data augmentation (synthetically expand training data). Batch norm and label smoothing also have regularizing side effects. All fight variance.' },
  ],
  'random-forests': [
    { id: 'mlfc_rf_1', q: 'What two sources of randomness make a random forest work?', a: 'Bootstrap sampling — each tree trains on a random sample drawn with replacement — and random feature subsets at every split (typically sqrt(d) for classification). Both decorrelate the trees; averaging many decorrelated overfit trees cancels their individual noise.' },
    { id: 'mlfc_rf_2', q: 'Why does averaging many deep trees reduce variance without adding much bias?', a: 'Each deep tree has low bias (it can fit anything) but high variance (it memorizes its sample\'s noise). Averaging n decorrelated estimators divides variance roughly by n while leaving expected predictions — hence bias — essentially unchanged. Decorrelation is critical: averaging identical trees would cancel nothing.', hint: 'Ask 300 slightly different experts and average' },
    { id: 'mlfc_rf_3', q: 'What is out-of-bag (OOB) evaluation?', a: 'Each bootstrap sample leaves out roughly 37 percent of rows. Every row can be scored by only the trees that never trained on it, yielding a free, built-in validation estimate (oob_score_ in sklearn) without sacrificing data to a hold-out split.' },
    { id: 'mlfc_rf_4', q: 'Which random forest hyperparameters matter most, and does adding trees cause overfitting?', a: 'n_estimators (more trees = better until it plateaus — more trees do NOT overfit, they only cost compute), max_features (controls decorrelation), and max_depth / min_samples_leaf (per-tree complexity). Contrast with boosting, where more rounds absolutely can overfit.' },
    { id: 'mlfc_rf_5', q: 'How does a random forest measure feature importance, and what is the known bias to watch for?', a: 'Impurity-based importance sums each feature\'s impurity reduction across all splits and trees — but it inflates high-cardinality and continuous features. Permutation importance (shuffle one feature, measure the score drop) is more reliable; SHAP values give consistent per-prediction attributions.' },
  ],
  'backpropagation': [
    { id: 'mlfc_bp_1', q: 'What problem does backpropagation solve, and what math powers it?', a: 'It computes the gradient of the loss with respect to EVERY weight in a network efficiently, so gradient descent knows how to update each one. It is the chain rule applied systematically backward through the computation graph, reusing intermediate results layer by layer.', hint: 'Chain rule + dynamic programming' },
    { id: 'mlfc_bp_2', q: 'Describe the forward and backward passes in one sentence each.', a: 'Forward: input flows through the layers, each caching its activations, ending in a loss value. Backward: starting from dLoss/dOutput, each layer multiplies by its local derivative to pass the gradient upstream, computing dLoss/dW for its own weights along the way.' },
    { id: 'mlfc_bp_3', q: 'Why is backprop dramatically cheaper than computing each weight\'s gradient independently?', a: 'Naive finite differences need one forward pass per weight — millions of passes. Backprop shares work: one forward plus one backward pass (about the cost of two forwards) yields ALL gradients, because the chain rule lets every layer reuse the gradient already computed downstream. It is dynamic programming on the computation graph.' },
    { id: 'mlfc_bp_4', q: 'What are vanishing and exploding gradients, and what causes them?', a: 'Backprop multiplies many layer derivatives together. If those factors are mostly below 1 the product shrinks exponentially — early layers get near-zero gradients and stop learning (vanishing). If above 1 it grows exponentially and training blows up (exploding). Deeper networks make both worse.' },
    { id: 'mlfc_bp_5', q: 'Name four standard fixes for vanishing/exploding gradients.', a: 'ReLU-family activations (derivative 1 on the active side, unlike sigmoid\'s max 0.25); careful initialization (Xavier/He) to keep signal scale stable; residual/skip connections that give gradients a highway past layers; batch normalization; and gradient clipping to cap explosions (standard in RNNs).' },
  ],
}
