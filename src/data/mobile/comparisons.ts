import type { Comparison } from '@/types'

/** Core mobile "X vs Y" comparisons every mobile engineer must be able to defend, framework-agnostic. */
export const MOBILE_COMPARISONS: Comparison[] = [
  {
    id: 'native-flutter-rn-kmp',
    title: 'Native vs Flutter vs React Native vs KMP',
    subtitle: 'The four ways to build a cross-platform-capable app',
    category: 'Frameworks',
    contenders: ['Native (Swift/Kotlin)', 'Flutter', 'React Native', 'KMP (Kotlin Multiplatform)'],
    rows: [
      { dimension: 'Codebase', values: ['One per platform (two teams/codebases)', 'One shared codebase for UI + logic', 'One shared codebase for UI + logic', 'Shared logic, native UI per platform'] },
      { dimension: 'UI rendering', values: ['Native widgets directly', 'Own engine (Skia/Impeller) draws every pixel', 'Native components driven from JS', 'Native UI (SwiftUI/Compose) you write per platform'] },
      { dimension: 'Language', values: ['Swift / Kotlin', 'Dart', 'JavaScript / TypeScript', 'Kotlin'] },
      { dimension: 'Performance ceiling', values: ['Highest — no abstraction layer', 'High and consistent; own render loop', 'Good; interop cost across the bridge/JSI', 'Native UI = native performance'] },
      { dimension: 'Platform fidelity / new APIs', values: ['Immediate, full access', 'Via plugins; may lag new OS features', 'Via native modules; may lag', 'Full — UI is native by design'] },
      { dimension: 'App size cost', values: ['Smallest baseline', 'Ships a runtime + engine (larger)', 'Ships a JS runtime (larger)', 'Small — no UI runtime overhead'] },
      { dimension: 'Best-fit team', values: ['Two platform teams, feel-critical apps', 'One team, pixel-consistent product UI', 'Web/JS team, product-surface apps', 'Native teams wanting to share business logic'] },
    ],
    whenToUse: [
      { choice: 'Native', when: 'The app is feel-critical or platform-heavy (pro camera, AR, deep OS integration) and you can staff two codebases.' },
      { choice: 'Flutter', when: 'One team must ship a consistent, custom-branded UI on both stores with strong performance and you accept a larger binary.' },
      { choice: 'React Native', when: 'A JS/web-strong team is building product surface (feeds, forms, commerce) and values the JS ecosystem and hot reload.' },
      { choice: 'KMP', when: 'You want native UI per platform but to share the business/data layer in Kotlin without a full cross-platform UI framework.' },
    ],
    verdict: 'There is no universal winner — pick by how platform-heavy the app is and how your team is shaped. Native maxes fidelity at double the code; Flutter and React Native trade a runtime and some platform lag for one codebase; KMP shares logic while keeping UI native. Every cross-platform choice still writes native code for the hard 10 percent, so size that fraction before you commit.',
  },
  {
    id: 'arc-vs-tracing-gc',
    title: 'ARC (iOS) vs Tracing GC (Android)',
    subtitle: 'Two fundamentally different ways to reclaim memory',
    category: 'Memory',
    contenders: ['ARC (reference counting)', 'Tracing GC (ART)'],
    rows: [
      { dimension: 'How it frees memory', values: ['Frees the instant strong ref count hits zero', 'Periodically walks the object graph, reclaims unreachable objects'] },
      { dimension: 'Timing', values: ['Deterministic — you know when it happens', 'Non-deterministic — GC runs when the runtime decides'] },
      { dimension: 'Reference cycles', values: ['Leak unless you break them with weak/unowned', 'Collected automatically — reachability, not counts'] },
      { dimension: 'Runtime cost', values: ['No collector; cost is inline retain/release', 'Background collector work + possible pauses'] },
      { dimension: 'Jank risk', values: ['No GC pauses; costs are spread out', 'A GC pause landing mid-frame can drop frames'] },
      { dimension: 'Classic leak pattern', values: ['Closure capturing self strongly (retain cycle)', 'Long-lived/static object pinning a screen or unremoved listener'] },
      { dimension: 'Developer burden', values: ['Must reason about ownership and capture lists', 'Must avoid holding references longer than needed and reduce churn'] },
    ],
    whenToUse: [
      { choice: 'ARC (reference counting)', when: 'The iOS model — think in ownership: mark the back-reference weak or unowned to break cycles, especially in closures that capture self.' },
      { choice: 'Tracing GC (ART)', when: 'The Android model — think in reachability: null out long-lived references, remove listeners, and minimize per-frame allocations to cut GC pressure.' },
    ],
    verdict: 'Neither model makes leaks impossible. ARC frees deterministically but leaks on cycles you must break by hand; tracing GC handles cycles but can jank on pauses and punishes allocation churn. In both, the real-world leak is the same shape: something long-lived holding a reference it should have dropped. Know your platform metaphor and audit ownership accordingly.',
  },
  {
    id: 'imperative-vs-declarative-ui',
    title: 'Imperative UI vs Declarative UI',
    subtitle: 'Mutating views by hand vs describing state (UIKit/Views vs SwiftUI/Compose)',
    category: 'UI',
    contenders: ['Imperative (UIKit / Views)', 'Declarative (SwiftUI / Compose / RN)'],
    rows: [
      { dimension: 'Mental model', values: ['Hold view references and mutate them on events', 'Write a pure function from state to UI'] },
      { dimension: 'Who owns transitions', values: ['You — manually update each view on each path', 'The framework — it diffs state and re-renders the delta'] },
      { dimension: 'Source of truth', values: ['Can drift: UI and data get out of sync', 'Single source of truth; UI always reflects state'] },
      { dimension: 'Typical bug class', values: ['Forgot to update a view on some code path', 'Unnecessary re-renders / recomposition'] },
      { dimension: 'Boilerplate for dynamic UI', values: ['High — manual show/hide/update wiring', 'Low — describe the state, framework reconciles'] },
      { dimension: 'Performance concern', values: ['Cheap updates but easy to desync', 'Must scope state so you do not rebuild too much'] },
      { dimension: 'Examples', values: ['UIKit, Android Views, findViewById', 'SwiftUI, Jetpack Compose, React Native'] },
    ],
    whenToUse: [
      { choice: 'Imperative (UIKit / Views)', when: 'Maintaining existing native codebases, or fine-grained manual control of a complex custom view or animation.' },
      { choice: 'Declarative (SwiftUI / Compose / RN)', when: 'New apps and most product UI — you want state-driven screens, less wiring, and fewer "forgot to update" bugs.' },
    ],
    verdict: 'The industry has moved declarative because "UI is a function of state" eliminates a whole class of desync bugs. The tradeoff is a new discipline: a single source of truth flowing down, and understanding recomposition cost so you scope updates instead of rebuilding the world. Same rendering, different bug — trade forgotten-update bugs for over-render bugs.',
  },
  {
    id: 'keyvalue-vs-secure-vs-sqlite',
    title: 'Key-Value vs Secure Store vs SQLite',
    subtitle: 'Matching the storage engine to the data (Prefs/DataStore vs Keychain/Keystore vs DB)',
    category: 'Storage',
    contenders: ['Key-Value (Prefs / UserDefaults / DataStore)', 'Secure Store (Keychain / Keystore)', 'SQLite (Room / Core Data)'],
    rows: [
      { dimension: 'Data shape', values: ['Small flat key-value flags', 'Small secrets (tokens, keys)', 'Structured, relational, growing collections'] },
      { dimension: 'Security', values: ['Plain, readable on rooted/jailbroken device', 'Hardware-backed; key material never leaves secure element', 'Plain by default; encrypt (SQLCipher) if sensitive'] },
      { dimension: 'Query ability', values: ['Read by key only', 'Read by key only', 'Full SQL: filter, join, sort, index'] },
      { dimension: 'Scale', values: ['Kilobytes — do not abuse for lists', 'Tiny — a handful of secrets', 'Megabytes to gigabytes'] },
      { dimension: 'Typical use', values: ['Settings, feature flags, onboarding-seen', 'Auth tokens, encryption keys, credentials', 'Offline cache, message history, entities'] },
      { dimension: 'Cost of misuse', values: ['Storing a token here = security hole', 'Storing bulk data here = wrong tool, slow', 'Storing one flag here = needless complexity'] },
    ],
    whenToUse: [
      { choice: 'Key-Value (Prefs / UserDefaults / DataStore)', when: 'Small non-secret flags and preferences — dark mode, last tab, onboarding completed.' },
      { choice: 'Secure Store (Keychain / Keystore)', when: 'Anything secret — auth tokens, refresh tokens, encryption keys, credentials. Never plain prefs.' },
      { choice: 'SQLite (Room / Core Data)', when: 'Structured data you query, or a growing offline cache — messages, notes, catalog, sync state.' },
    ],
    verdict: 'Match the store to the data: small flags in key-value, secrets in the hardware-backed secure store, structured or large data in SQLite. The two classic mistakes are putting a token in plain SharedPreferences/UserDefaults (a real vulnerability on rooted devices) and abusing key-value storage as a database for lists it was never meant to hold.',
  },
  {
    id: 'workmanager-vs-bgtaskscheduler',
    title: 'WorkManager vs BGTaskScheduler',
    subtitle: 'How Android and iOS want you to run deferrable background work',
    category: 'Background',
    contenders: ['WorkManager (Android)', 'BGTaskScheduler (iOS)'],
    rows: [
      { dimension: 'What it is', values: ['Android Jetpack scheduler for deferrable, guaranteed work', 'iOS system scheduler for background refresh/processing tasks'] },
      { dimension: 'Survives process death', values: ['Yes — work persists and reruns across restarts/reboots', 'Task is registered; system launches your app to run it'] },
      { dimension: 'Who decides timing', values: ['System, honoring your constraints (Doze-aware)', 'System, based on usage patterns and power'] },
      { dimension: 'Constraints', values: ['Network type, charging, storage, idle', 'Requires network / external power (processing tasks)'] },
      { dimension: 'Guarantee', values: ['Guaranteed to run eventually (deferrable)', 'Best-effort within a granted window; not guaranteed soon'] },
      { dimension: 'Retry / backoff', values: ['Built-in retry with backoff policy', 'You reschedule the next task yourself'] },
      { dimension: 'Not for', values: ['Immediate or exact-time work (use alarms/foreground)', 'Long or user-visible work (use background URLSession)'] },
    ],
    whenToUse: [
      { choice: 'WorkManager (Android)', when: 'Deferrable, guaranteed Android work — sync, upload, cleanup — that must survive reboots and respect battery constraints.' },
      { choice: 'BGTaskScheduler (iOS)', when: 'Periodic iOS background refresh or maintenance the system runs opportunistically when the user is not active.' },
    ],
    verdict: 'Both encode the same hard truth: you do not own the background — the OS schedules deferrable work to protect battery. Hand it a durable, constrained job instead of spawning a thread and hoping. For user-visible ongoing transfers, escalate to a foreground service (Android) or a background URLSession (iOS); WorkManager and BGTaskScheduler are for deferrable, not immediate, work.',
  },
  {
    id: 'fcm-vs-apns',
    title: 'FCM vs APNs',
    subtitle: 'The two push notification pipelines',
    category: 'Push',
    contenders: ['FCM (Firebase Cloud Messaging)', 'APNs (Apple Push Notification service)'],
    rows: [
      { dimension: 'Platform', values: ['Android (and cross-platform via one API)', 'iOS / Apple platforms'] },
      { dimension: 'Token model', values: ['FCM registration token per app install', 'APNs device token per app install'] },
      { dimension: 'Delivery guarantee', values: ['Best-effort; may collapse/drop under Doze', 'Best-effort; throttled for low-priority pushes'] },
      { dimension: 'Payload types', values: ['Notification vs data messages (data can wake app logic)', 'Alert, background (content-available), silent pushes'] },
      { dimension: 'Who wakes the device', values: ['Google Play services delivers to the device', 'Apple push infrastructure delivers to the device'] },
      { dimension: 'Common role', values: ['Trigger sync, alerts; often fronts APNs too', 'Alerts, background refresh triggers'] },
      { dimension: 'Reliability caveat', values: ['Not a guaranteed transport — do not rely on every push', 'Priority matters; silent pushes are rate-limited'] },
    ],
    whenToUse: [
      { choice: 'FCM (Firebase Cloud Messaging)', when: 'Android push, or a single cross-platform push API — FCM can relay to APNs so your backend integrates once.' },
      { choice: 'APNs (Apple Push Notification service)', when: 'iOS push. Direct APNs integration when you want full control and no third-party layer between you and Apple.' },
    ],
    verdict: 'Different transports for different platforms, and neither is a guaranteed delivery channel — treat push as a hint to sync, not the payload itself. Many teams front both with FCM for one backend integration. Whatever you choose, design so a dropped or delayed push is harmless: the client still reconciles truth from the server on next open.',
  },
  {
    id: 'offline-first-vs-online-only',
    title: 'Offline-First vs Online-Only',
    subtitle: 'Is the network a background detail or a hard prerequisite?',
    category: 'Architecture',
    contenders: ['Offline-First', 'Online-Only'],
    rows: [
      { dimension: 'Source of truth for UI', values: ['Local database', 'The server (fetched on demand)'] },
      { dimension: 'Behavior with no signal', values: ['Fully usable; reads/writes local, syncs later', 'Blocked — spinners, errors, or empty screens'] },
      { dimension: 'Perceived speed', values: ['Instant — no network in the render path', 'Bound by round-trip latency'] },
      { dimension: 'Complexity', values: ['High — sync engine, conflict resolution, IDs, state', 'Low — request/response, server owns state'] },
      { dimension: 'Conflict handling', values: ['Required — concurrent edits across devices', 'Server serializes; last request wins naturally'] },
      { dimension: 'Best-fit apps', values: ['Notes, messaging, field tools, email', 'Banking transactions, live inventory, real-time trading'] },
      { dimension: 'Failure mode', values: ['Sync bugs can silently lose data', 'App is useless in a tunnel'] },
    ],
    whenToUse: [
      { choice: 'Offline-First', when: 'Users work in poor connectivity or expect instant response, and stale-but-present beats absent — notes, chat, field/logistics apps.' },
      { choice: 'Online-Only', when: 'Data must be authoritative and current, or the domain forbids stale local writes — payments, live inventory, trading.' },
    ],
    verdict: 'Offline-first buys a fast, resilient app at the cost of a real sync engine and conflict resolution — do not adopt it lightly, but its payoff is enormous for content and productivity apps. Online-only is simpler and correct where staleness is unacceptable. The decision is about whether stale-but-instant beats fresh-but-blocked for your domain.',
  },
  {
    id: 'rest-vs-graphql-mobile',
    title: 'REST vs GraphQL on Mobile',
    subtitle: 'Fetching exactly what a bandwidth-constrained screen needs',
    category: 'Networking',
    contenders: ['REST', 'GraphQL'],
    rows: [
      { dimension: 'Data fetching', values: ['Fixed endpoints; server decides the shape', 'Client asks for exactly the fields it needs'] },
      { dimension: 'Over-fetching', values: ['Common — endpoints return more than a screen uses', 'Minimal — request only what renders'] },
      { dimension: 'Round trips per screen', values: ['Often several (N+1 endpoints)', 'One query can assemble a whole screen'] },
      { dimension: 'Payload size on cellular', values: ['Larger; wastes constrained bandwidth', 'Smaller; tailored to the view'] },
      { dimension: 'Caching', values: ['Simple HTTP caching by URL', 'Harder; needs normalized client cache (Apollo/urql)'] },
      { dimension: 'Versioning', values: ['Explicit /v2 endpoints', 'Schema evolves; deprecate fields instead'] },
      { dimension: 'Tooling / cost', values: ['Ubiquitous, trivial to consume', 'Powerful but heavier client + server setup'] },
    ],
    whenToUse: [
      { choice: 'REST', when: 'Simple resource CRUD, heavy reliance on HTTP/CDN caching, or a small API where over-fetching is not hurting you.' },
      { choice: 'GraphQL', when: 'Complex, screen-driven UIs on cellular where trimming payloads and collapsing many calls into one query materially improves performance.' },
    ],
    verdict: 'On mobile, bandwidth and latency are the real constraints, and GraphQL directly attacks over-fetching and chatty N+1 round trips — a big win for rich, screen-composed UIs. But it adds client and server complexity and loses easy HTTP caching. Use REST for simple, cache-friendly resources; reach for GraphQL when tailoring payloads per screen actually moves the needle.',
  },
  {
    id: 'cold-warm-hot-start',
    title: 'Cold vs Warm vs Hot Start',
    subtitle: 'The three launch paths and why the first frame feels slow',
    category: 'Performance',
    contenders: ['Cold Start', 'Warm Start', 'Hot Start'],
    rows: [
      { dimension: 'Process state at launch', values: ['No process — created from scratch', 'Process may be alive; UI must be rebuilt', 'Process alive and UI in memory'] },
      { dimension: 'What the OS does', values: ['Fork process, load & link binary, init runtime, then app code', 'Recreate activity/scene and first frame', 'Bring existing UI back to foreground'] },
      { dimension: 'Relative speed', values: ['Slowest', 'Medium', 'Fastest — near-instant'] },
      { dimension: 'Typical trigger', values: ['First launch, or after the OS killed the process', 'Reopen after background kill of UI only', 'Reopen shortly after backgrounding'] },
      { dimension: 'What you can control', values: ['Defer init, shrink binary, draw skeleton fast', 'Restore state cheaply; avoid rebuild work', 'Little — mostly free'] },
      { dimension: 'Where time goes', values: ['SDK/DI init, main-thread IO, big JS bundle', 'View recreation + state restore', 'Negligible'] },
    ],
    whenToUse: [
      { choice: 'Cold Start', when: 'Optimize this first — it is the worst case and what users mean by "slow to open." Defer everything not needed for the first frame.' },
      { choice: 'Warm Start', when: 'Keep state restoration cheap so a background-killed app rebuilds its UI quickly without redoing full init.' },
      { choice: 'Hot Start', when: 'Already fast; just avoid doing heavy work in resume callbacks that would stall bringing the app forward.' },
    ],
    verdict: 'Cold start is the case that earns complaints and the one you budget for — every eager SDK init, main-thread read, and oversized bundle is paid before the first frame. Defer everything non-essential, draw a skeleton fast for perceived speed, and guard a startup budget in CI. Warm and hot start are cheaper variants where a live process saves you most of the work.',
  },
  {
    id: 'aab-vs-apk-thinning',
    title: 'App Bundle (AAB) vs APK vs App Thinning',
    subtitle: 'Shipping one build but installing only what a device needs',
    category: 'Release',
    contenders: ['Universal APK', 'Android App Bundle (AAB)', 'iOS App Thinning'],
    rows: [
      { dimension: 'What you upload', values: ['A single APK with everything for all devices', 'A bundle; the store generates per-device APKs', 'One binary with slices/on-demand resources'] },
      { dimension: 'What the device installs', values: ['Everything — all densities, all ABIs', 'Only the code/resources for that device', 'Only the slice matching that device'] },
      { dimension: 'Install size', values: ['Largest — carries unused assets', 'Smaller — tailored per device', 'Smaller — variant + app-thinned resources'] },
      { dimension: 'Who splits it', values: ['Nobody — one-size-fits-all', 'Google Play (dynamic delivery)', 'App Store (slicing)'] },
      { dimension: 'Signing implication', values: ['You sign the final APK', 'Play App Signing signs generated APKs from your key', 'Standard iOS signing / provisioning'] },
      { dimension: 'Extra capability', values: ['Simple, no server logic', 'On-demand / dynamic feature modules', 'On-demand resources loaded when needed'] },
    ],
    whenToUse: [
      { choice: 'Universal APK', when: 'Sideloading, CI artifacts, or distribution outside the store where you need one file that runs anywhere.' },
      { choice: 'Android App Bundle (AAB)', when: 'Standard Play Store distribution — required for new apps; delivers the smallest per-device install and enables dynamic features.' },
      { choice: 'iOS App Thinning', when: 'Standard App Store distribution — slicing and on-demand resources deliver only what each iPhone/iPad model needs.' },
    ],
    verdict: 'Per-device delivery is the modern default: upload once, let the store ship each device only the code and resources it can use. AAB (Play) and app thinning (App Store) both cut install size and startup cost versus a universal APK. Keep a universal APK only for sideloading or out-of-store distribution where dynamic delivery is not available.',
  },
]
