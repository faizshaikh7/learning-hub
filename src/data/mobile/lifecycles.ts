import type { Lifecycle } from '@/types'

/** Core mobile lifecycles every mobile engineer must know cold, framework-agnostic. */
export const MOBILE_LIFECYCLES: Lifecycle[] = [
  {
    id: 'app-activity-lifecycle',
    title: 'The App / Activity Lifecycle',
    subtitle: 'Cold start → foreground → background → process death → back again',
    icon: '📱',
    flow: 'cyclic',
    overview:
      'A mobile app is never simply "running". The OS drives it through states — launching, foreground, background, suspended — and can freeze or kill it at any time to reclaim memory and battery. The single most important consequence: backgrounded means "about to be suspended", and a killed process gets no goodbye callback. Save state early, and design so a fresh process can rebuild exactly where the user left off.',
    stages: [
      {
        name: 'Not running',
        shortLabel: 'not running',
        description: 'The app has no process. It has either never launched this session or was terminated from the background.',
        durationHint: 'until launched',
        details: [
          'No code of yours is executing and no memory is held',
          'The OS may have killed a previous process to reclaim memory — this is normal, not a crash',
          'Any state that was only in memory is already gone; only persisted state remains',
        ],
      },
      {
        name: 'Launching (cold start)',
        shortLabel: 'launch',
        description: 'The OS forks a new process, initializes the runtime, and runs your app entry point before any screen exists.',
        durationHint: 'hundreds of ms',
        details: [
          'Process creation → runtime init → app entry point (Application / AppDelegate / main) → first screen built and drawn',
          'This is the critical path for perceived launch speed; every eager SDK init delays first frame',
          'A system launch/splash screen covers the window until your first real frame is ready',
        ],
        gotchas: [
          'Heavy synchronous work in the app entry point (analytics, DI, SDK init) runs on the main thread and directly delays time-to-interactive',
        ],
      },
      {
        name: 'Foreground / active',
        shortLabel: 'foreground',
        description: 'The app is visible and receiving user input — the only state where it can assume full resources.',
        durationHint: 'while in use',
        details: [
          'The UI thread event loop is processing input and rendering frames within the frame budget',
          'This is when to start visibility-scoped resources: location, sensors, animations, active network',
          'Android: onResume / STARTED-RESUMED · iOS: scene didBecomeActive',
        ],
      },
      {
        name: 'Inactive / paused',
        shortLabel: 'inactive',
        description: 'Visible but not focused — a call banner, control center, permission dialog, or the app switcher is overlaid.',
        durationHint: 'transient',
        details: [
          'Pause high-frequency work: animations, camera preview, game loops',
          'This is a brief transitional state on the way to foreground or background',
          'Android: onPause (partial) · iOS: scene willResignActive',
        ],
      },
      {
        name: 'Background',
        shortLabel: 'background',
        description: 'Not visible. The app gets a short window to finish work before the OS suspends it.',
        durationHint: 'seconds, then frozen',
        details: [
          'Persist critical/unsaved state NOW — this is the last guaranteed moment before suspension or death',
          'Stop battery-draining work (GPS, sensors); release resources you do not need',
          'Android: onStop / SavedStateHandle · iOS: scene didEnterBackground + a short background task assertion',
        ],
        gotchas: [
          'Assuming timers or in-flight network calls keep running in the background — the process may be frozen mid-flight',
        ],
      },
      {
        name: 'Suspended → process death',
        shortLabel: 'killed',
        description: 'The process is frozen and, under memory pressure, silently terminated with no further callbacks.',
        durationHint: 'no warning',
        details: [
          'A killed app receives NO onDestroy / final callback — never rely on one to save state',
          'All in-memory state is lost; only what you persisted (disk, saved-instance-state, server) survives',
          'The OS kills background apps first, so higher memory footprint means earlier death',
        ],
        gotchas: [
          'Storing session/nav state only in a singleton or variable — it vanishes on process death and the app relaunches broken',
        ],
      },
      {
        name: 'Restoration (loop back)',
        shortLabel: 'restore',
        description: 'On return, the OS starts a fresh process and expects the app to look like the user never left.',
        durationHint: 'must feel seamless',
        details: [
          'Rebuild the UI from saved-instance-state / state restoration, and rehydrate data from disk or server',
          'Reconstruct the navigation back stack so system back behaves correctly',
          'The acid test: kill the app from the background, relaunch, and land exactly where the user was',
        ],
      },
    ],
    keyTakeaways: [
      'Backgrounded means "about to be suspended" — save critical state as you leave the foreground, not later',
      'A killed process gets no final callback; only persisted state survives process death',
      'Perceived launch speed is decided on the cold-start critical path — defer non-essential init off it',
      'Design every screen to be reconstructible from a small serializable state bundle',
    ],
    interviewNotes: [
      'Be able to name the state transitions on both Android (onCreate/onStart/onResume/onPause/onStop/onDestroy) and iOS (scene phases)',
      'Explain why onDestroy is not guaranteed and what you do instead',
      'Describe the difference between a configuration change (recreation) and true process death',
    ],
    relatedTopics: ['app-lifecycle-states', 'process-death-restoration', 'app-launch-types', 'config-changes'],
  },
  {
    id: 'screen-viewcontroller-lifecycle',
    title: 'The Screen / ViewController Lifecycle',
    subtitle: 'Create → appear → disappear → destroy — start and stop work symmetrically',
    icon: '🖼️',
    flow: 'cyclic',
    overview:
      'Within a running app, each screen has its own lifecycle as the user navigates in and out. The core discipline is symmetry: every resource you start when a screen becomes visible must be stopped when it is hidden, or you leak memory and drain battery on screens the user cannot even see. Screens also survive being off-screen (kept on the back stack) and can be recreated on configuration changes.',
    stages: [
      {
        name: 'Create / load',
        shortLabel: 'create',
        description: 'The screen and its view hierarchy are constructed and its state holder is initialized.',
        durationHint: 'once per instance',
        details: [
          'Build the view tree and wire up the state holder / view model (which may outlive the view)',
          'Do lightweight setup only — heavy or blocking work here delays the screen appearing',
          'Android: Fragment onCreate/onViewCreated · iOS: viewDidLoad',
        ],
        gotchas: ['Doing network or disk work synchronously here freezes the transition into the screen'],
      },
      {
        name: 'Will appear',
        shortLabel: 'will appear',
        description: 'The screen is about to become visible; prepare UI state before the transition animation runs.',
        durationHint: 'each appearance',
        details: [
          'Refresh data that may have changed while away, update UI to current state',
          'Called every time the screen re-appears (e.g. popping back to it), not just the first time',
          'Android: onStart · iOS: viewWillAppear',
        ],
      },
      {
        name: 'Appeared / active',
        shortLabel: 'appeared',
        description: 'The screen is fully visible and interactive — start visibility-scoped work here.',
        durationHint: 'while visible',
        details: [
          'Begin observers, location/sensor updates, animations, and analytics "screen view" here',
          'Subscribe to reactive data streams so the UI stays in sync with the source of truth',
          'Android: onResume · iOS: viewDidAppear',
        ],
      },
      {
        name: 'Will disappear',
        shortLabel: 'will disappear',
        description: 'The screen is leaving (navigating forward or being popped); commit and pause work.',
        durationHint: 'each departure',
        details: [
          'Commit unsaved input, dismiss the keyboard, pause animations',
          'The screen may still be alive on the back stack — this is not necessarily destruction',
          'Android: onPause · iOS: viewWillDisappear',
        ],
      },
      {
        name: 'Disappeared / stopped',
        shortLabel: 'stopped',
        description: 'The screen is no longer visible. Stop everything that only makes sense while on screen.',
        durationHint: 'until re-shown or destroyed',
        details: [
          'STOP location, sensors, camera, timers, and observers — running them off-screen wastes battery and leaks',
          'Every start in "appeared" must have a matching stop here (lifecycle symmetry)',
          'Android: onStop · iOS: viewDidDisappear',
        ],
        gotchas: ['Forgetting to unregister listeners/observers is the classic memory leak and battery-drain bug'],
      },
      {
        name: 'Destroy (loop back on renavigate)',
        shortLabel: 'destroy',
        description: 'The screen is removed for good (popped off the stack) or recreated on a configuration change.',
        durationHint: 'once or on recreate',
        details: [
          'Release remaining resources and cancel outstanding work tied to this screen',
          'On a config change the view is destroyed and recreated, but a scoped state holder can survive to avoid reloading',
          'Android: onDestroyView/onDestroy · iOS: deinit',
        ],
        gotchas: ['Holding a reference to a destroyed view (e.g. in a long-lived callback) leaks the whole screen'],
      },
    ],
    keyTakeaways: [
      'Symmetry is the rule: every start in an "appear" callback needs a matching stop in a "disappear" callback',
      'Appear/disappear fire on every navigation, but create/destroy may fire only once — scope work accordingly',
      'A screen off-screen on the back stack still exists; hidden is not destroyed',
      'Keep durable state in a lifecycle-aware holder that survives view recreation on config changes',
    ],
    interviewNotes: [
      'Explain the difference between a view being paused/stopped versus destroyed',
      'Give a concrete example of a lifecycle-driven memory leak and how you would fix it',
      'Describe how a view model / state holder outlives the view to survive rotation',
    ],
    relatedTopics: ['screen-lifecycle', 'app-lifecycle-states', 'config-changes', 'state-management-concepts'],
  },
  {
    id: 'frame-rendering-pipeline',
    title: 'The Frame Rendering Pipeline',
    subtitle: 'measure → layout → draw → rasterize → composite, every ~16ms',
    icon: '🎞️',
    flow: 'linear',
    overview:
      'Every visible frame is produced by the same pipeline, and it must finish within the frame budget — about 16.7ms at 60Hz, ~8ms at 120Hz — or the frame is dropped and the user sees jank. The pipeline is split across CPU (measure, layout, record draw commands) and GPU (rasterize, composite). Smooth apps keep per-frame work small and animate properties that only touch the compositing stage.',
    stages: [
      {
        name: 'Input & state update',
        shortLabel: 'input',
        description: 'The UI thread processes input events and your logic updates the UI state for this frame.',
        durationHint: 'start of frame',
        details: [
          'Touch/gesture events and callbacks run on the UI thread and may change state',
          'In declarative UI, changed state marks parts of the tree dirty for rebuild',
          'Any heavy work here (parsing, allocation) eats directly into the frame budget',
        ],
        gotchas: ['Doing synchronous I/O or large allocations in an input handler stalls the whole frame'],
      },
      {
        name: 'Measure',
        shortLabel: 'measure',
        description: 'Each element computes how big it wants to be, walking the view/widget tree.',
        durationHint: 'CPU',
        details: [
          'Sizing constraints flow down the tree and desired sizes flow back up',
          'Deep or complex trees make this pass expensive; flatter hierarchies measure faster',
          'Repeated/nested measurement passes (e.g. some weighted layouts) can multiply the cost',
        ],
      },
      {
        name: 'Layout',
        shortLabel: 'layout',
        description: 'Given measured sizes, every element is assigned a final position within its parent.',
        durationHint: 'CPU',
        details: [
          'Positions are computed top-down after measurement resolves sizes',
          'Layout is re-run whenever geometry changes (size, insertion, text reflow)',
          'Animating layout-affecting properties (width, position via layout) forces this pass every frame',
        ],
      },
      {
        name: 'Draw / record',
        shortLabel: 'draw',
        description: 'The UI records drawing commands (shapes, text, images) for each element into a display list.',
        durationHint: 'CPU',
        details: [
          'This produces GPU instructions, not pixels yet — it is recording what to paint',
          'Overdraw (painting the same pixel many times through stacked opaque layers) is created here',
          'Expensive custom drawing or large text/shadow work shows up as a long draw phase',
        ],
      },
      {
        name: 'Rasterize',
        shortLabel: 'raster',
        description: 'The GPU turns the recorded draw commands into actual pixels in a buffer.',
        durationHint: 'GPU',
        details: [
          'Large textures, decoding big images inline, and heavy shaders/blur make this pass slow',
          'A GPU-bound frame shows a long raster phase even when CPU work is small',
          'Downsampling images to display size keeps rasterization cheap',
        ],
        gotchas: ['Decoding a full-resolution image on the render path spikes raster time and can drop frames'],
      },
      {
        name: 'Composite & present',
        shortLabel: 'composite',
        description: 'Rendered layers are combined (with transforms, opacity, clips) and the final buffer is handed to the display at VSync.',
        durationHint: 'GPU, synced to VSync',
        details: [
          'Transform/opacity animations on existing layers happen here without re-running layout or paint — that is why they are cheap and smooth',
          'Double/triple buffering swaps the finished buffer to screen at the refresh signal to avoid tearing',
          'Too many blended/overlapping layers make compositing itself a bottleneck',
        ],
      },
    ],
    keyTakeaways: [
      'ALL per-frame work (CPU + GPU) must fit the budget: ~16.7ms at 60Hz, ~8ms at 120Hz',
      'CPU owns measure/layout/draw; GPU owns raster/composite — a slow frame can be bound on either side',
      'Animate transform and opacity (compositing-only) to stay smooth; avoid animating layout-affecting properties',
      'Users perceive the worst frames, not the average — chase frame-time consistency, not just high FPS',
    ],
    interviewNotes: [
      'Explain why animating transform/opacity is cheaper than animating width/height',
      'Describe how you would determine if a janky scroll is CPU-bound or GPU-bound',
      'Define overdraw and give two ways to reduce it',
    ],
    relatedTopics: ['frame-budget-jank', 'gpu-compositing', 'list-virtualization', 'render-performance'],
  },
  {
    id: 'mobile-network-request-lifecycle',
    title: 'The Mobile Network Request Lifecycle',
    subtitle: 'Build → connect → send → wait → parse — with retry, cache, and an offline queue',
    icon: '🌐',
    flow: 'linear',
    overview:
      'On mobile, a network request is a gauntlet: the radio may be asleep, latency is high, and the connection can drop mid-response. A robust request path checks connectivity, reuses connections, applies timeouts and cancellation, serves cache when possible, retries transient failures with backoff, and queues writes made offline. The network is assumed unreliable, so failure handling is the main event, not an afterthought.',
    stages: [
      {
        name: 'Build & check policy',
        shortLabel: 'build',
        description: 'Construct the request and decide whether to hit the network, the cache, or both.',
        durationHint: 'instant',
        details: [
          'Attach auth token (from secure storage), headers, and an idempotency key for writes',
          'Check connectivity and cache freshness — you may serve cache immediately and skip the network',
          'Set connect, read, and overall timeouts up front so a stalled request cannot hang forever',
        ],
        gotchas: ['Firing a request without checking cache first causes needless latency and battery/radio use'],
      },
      {
        name: 'Establish connection',
        shortLabel: 'connect',
        description: 'DNS resolve, wake the radio if idle, open the transport (TCP/QUIC), and complete the TLS handshake.',
        durationHint: 'radio wakeup + handshake',
        details: [
          'On cellular after idle, the radio must power up (radio-state promotion) — hundreds of ms before any bytes flow',
          'Reuse connections (keep-alive, HTTP/2 multiplexing) to avoid repeating handshakes',
          'TLS authenticates the server; high-security apps also verify against pinned certificates/keys here',
        ],
        gotchas: ['One request per idle period repeatedly wakes the radio — batch requests to amortize the cost'],
      },
      {
        name: 'Send & wait',
        shortLabel: 'send',
        description: 'Transmit the request and await the response across a high-latency, possibly lossy link.',
        durationHint: 'server + network latency',
        details: [
          'This is where most time goes; the request can also drop mid-flight here',
          'Cancel the request if the user leaves the screen (structured concurrency / cancellation tokens)',
          'Stream large responses rather than buffering the whole body in memory',
        ],
        gotchas: ['Not cancelling requests on navigation wastes battery and can update a destroyed screen'],
      },
      {
        name: 'Receive & parse',
        shortLabel: 'parse',
        description: 'Read the response and deserialize it — off the main thread — into domain objects.',
        durationHint: 'off-main CPU',
        details: [
          'Parsing large JSON/payloads on the main thread causes jank — always parse on a background thread',
          'Validate and map to models; handle partial or malformed responses defensively',
          'Update the local store (source of truth) so observing UI refreshes reactively',
        ],
      },
      {
        name: 'Handle failure & retry',
        shortLabel: 'retry',
        description: 'On transient errors, retry with exponential backoff and jitter; on permanent errors, surface them.',
        durationHint: 'backoff delays',
        details: [
          'Retry only transient/idempotent failures (timeouts, 5xx, 429 with Retry-After); do not blindly retry 4xx or unsafe writes',
          'Exponential backoff avoids hammering a struggling server; jitter avoids synchronized retry storms',
          'Idempotency keys make write retries safe against lost-response duplicates',
        ],
        gotchas: ['Retrying a non-idempotent write without an idempotency key causes duplicate side effects (double charge)'],
      },
      {
        name: 'Cache & offline queue',
        shortLabel: 'cache/queue',
        description: 'Cache successful reads; when offline, enqueue writes durably to replay when connectivity returns.',
        durationHint: 'persists across restarts',
        details: [
          'Store read results with a freshness policy so the next launch/scroll is instant and offline-capable',
          'Queue offline writes in a durable outbox that survives app restart and process death',
          'A sync worker flushes the outbox with backoff when the network returns, marking each item done or failed',
        ],
      },
    ],
    keyTakeaways: [
      'Assume the network is slow, intermittent, and will fail — failure handling is the core of the design',
      'Always set timeouts and cancel in-flight requests tied to a screen the user has left',
      'Retry transient failures with exponential backoff + jitter; make write retries safe with idempotency keys',
      'Cache reads and queue writes so the app is fast and functional even offline',
    ],
    interviewNotes: [
      'Explain radio-state promotion and why the first request after idle is slow on cellular',
      'Describe which HTTP failures are safe to retry and why idempotency keys matter for writes',
      'Sketch an offline write queue (outbox) and how it survives process death',
    ],
    relatedTopics: ['mobile-request-lifecycle', 'flaky-network-design', 'offline-first', 'caching-strategies-mobile'],
  },
  {
    id: 'push-notification-pipeline',
    title: 'The Push Notification Delivery Pipeline',
    subtitle: 'Register → token → your server → FCM/APNs → device (best-effort)',
    icon: '🔔',
    flow: 'linear',
    overview:
      'A suspended app has no live connection, so push relies on ONE persistent system socket shared by all apps — FCM on Android, APNs on iOS — that the OS keeps open efficiently. Your server hands a message plus a device token to that push service, which routes it to the device and wakes your app or shows the notification. Delivery is best-effort: it may arrive late, once, or never, so push is a hint to sync, not a reliable data channel.',
    stages: [
      {
        name: 'Register for push',
        shortLabel: 'register',
        description: 'The app asks the OS/push service to register and (on iOS) requests notification permission.',
        durationHint: 'once, refreshed',
        details: [
          'iOS requires explicit user opt-in for notifications; Android 13+ also requires runtime permission',
          'The app registers with FCM/APNs to obtain a unique device token',
          'Handle the user declining permission gracefully — push is opt-in and revocable',
        ],
      },
      {
        name: 'Obtain & upload token',
        shortLabel: 'token',
        description: 'The push service returns a device token; the app sends it to your backend to store.',
        durationHint: 'on register / refresh',
        details: [
          'The token uniquely addresses this app install on this device for the push service',
          'Tokens rotate/expire — listen for refresh and re-upload, and prune stale tokens server-side',
          'Associate the token with the user/session so you can target the right device',
        ],
        gotchas: ['Not handling token refresh leads to silently undeliverable notifications over time'],
      },
      {
        name: 'Server sends message',
        shortLabel: 'server send',
        description: 'Your backend calls FCM/APNs with the device token, payload, and priority.',
        durationHint: 'on event',
        details: [
          'Choose type: a notification message (OS displays it) or a data/silent push (wakes the app to run code)',
          'Set priority appropriately; high priority can wake through low-power modes but must be justified',
          'Keep payloads small and treat them as triggers to fetch, not as the authoritative data',
        ],
      },
      {
        name: 'Push service routes',
        shortLabel: 'route',
        description: 'FCM/APNs delivers the message to the device over its persistent, battery-efficient system connection.',
        durationHint: 'best-effort',
        details: [
          'One OS-maintained socket serves all apps, which is what makes push power-efficient',
          'If the device is offline, the service may store-and-forward or coalesce/drop messages',
          'Low-power modes (Doze/App Nap) can delay or throttle delivery',
        ],
        gotchas: ['Delivery is not guaranteed or ordered — never assume every push arrives exactly once'],
      },
      {
        name: 'Device wakes app',
        shortLabel: 'wake',
        description: 'The OS wakes your app (data push) or renders the notification (notification message).',
        durationHint: 'brief',
        details: [
          'Notification messages: the system shows the alert even if the app is not running',
          'Data/silent pushes: the OS gives the app a short, throttled window to run background code',
          'Background execution here is time-limited — do minimal work, then let the app suspend again',
        ],
      },
      {
        name: 'App handles & syncs',
        shortLabel: 'handle',
        description: 'The app reconciles with your server as the source of truth and updates local state.',
        durationHint: 'on tap / on receipt',
        details: [
          'On tap, deep-link to the relevant screen and rebuild a sensible back stack',
          'Fetch the real data from your server rather than trusting the payload alone',
          'Design for missed and duplicate notifications — dedupe and sync to converge on correct state',
        ],
      },
    ],
    keyTakeaways: [
      'Push works because ONE OS socket (FCM/APNs) serves all apps efficiently — a suspended app has no socket of its own',
      'The flow is register → token → your server → FCM/APNs → device; keep tokens fresh',
      'Delivery is best-effort: late, once, or never — treat push as a hint to sync, not a reliable transport',
      'Silent/background pushes are heavily throttled and cannot be your only sync mechanism',
    ],
    interviewNotes: [
      'Explain why the app cannot maintain its own persistent connection for push',
      'Describe the difference between notification and data/silent messages and their OS limits',
      'Explain why you must sync from the server after a push rather than trusting the payload',
    ],
    relatedTopics: ['push-notifications-pipeline', 'background-sync', 'background-execution-limits', 'deep-linking-mobile'],
  },
  {
    id: 'build-sign-release-pipeline',
    title: 'The Build → Sign → Release → Review → Rollout Pipeline',
    subtitle: 'From source commit to a staged production rollout on the stores',
    icon: '🚀',
    flow: 'linear',
    overview:
      'Shipping mobile is not "deploy and done". Every release is built for two native platforms, signed with tightly guarded keys, tested, submitted to a store, gated by review that can take hours to days, and then rolled out in stages so a bad build can be halted before it reaches everyone. Because you cannot instantly hotfix a live app, feature flags and staged rollout are how you de-risk shipping.',
    stages: [
      {
        name: 'Commit & CI build',
        shortLabel: 'build',
        description: 'A change triggers CI to build release artifacts for both platforms with the right configuration.',
        durationHint: 'minutes',
        details: [
          'Build the release variant: minified/obfuscated (R8/ProGuard), optimized, debug logging stripped',
          'iOS builds require macOS runners and Xcode; Android needs the SDK/NDK toolchain',
          'Auto-increment the build/version number and tag the commit for traceability',
        ],
        gotchas: ['Uploading a build number not higher than the last gets rejected by the store'],
      },
      {
        name: 'Test & verify',
        shortLabel: 'test',
        description: 'Run unit, integration, and a small set of UI tests, plus lint and static analysis, before signing off.',
        durationHint: 'minutes to tens of minutes',
        details: [
          'Fast unit tests form the base; a few UI/E2E tests cover critical journeys on emulators or a device farm',
          'Static analysis and dependency/security scans catch issues early',
          'Upload symbol/mapping files (dSYM, R8 mapping) so future crash reports can be symbolicated',
        ],
      },
      {
        name: 'Sign',
        shortLabel: 'sign',
        description: 'The artifact is cryptographically signed with the release key, proving authenticity and integrity.',
        durationHint: 'seconds',
        details: [
          'Signing keys/certs are stored securely in CI and access-controlled — losing them blocks in-place updates',
          'Updates must be signed with the same key as the installed app or users cannot upgrade',
          'Play App Signing / Apple managed certificates help custody but the key is a real operational risk',
        ],
        gotchas: ['A signature mismatch makes the store treat the upload as a different app — users cannot update in place'],
      },
      {
        name: 'Beta distribution',
        shortLabel: 'beta',
        description: 'The signed build goes to internal/closed testers to validate the exact artifact before public release.',
        durationHint: 'hours to days',
        details: [
          'TestFlight (iOS), Play internal/closed/open testing (Android), or Firebase App Distribution',
          'Testers exercise near-production builds to catch issues lab tests and review miss',
          'You promote the same artifact you tested — no rebuild between beta and production',
        ],
      },
      {
        name: 'Store review',
        shortLabel: 'review',
        description: 'The store reviews the app against policy: privacy, security, content, and functionality.',
        durationHint: 'hours to days',
        details: [
          'Apple review is stricter and human-involved; Google mixes automated and manual checks',
          'Prepare metadata: screenshots, privacy nutrition labels / Data Safety, age rating, localized listings',
          'Common rejections: undisclosed data collection, crashes found in review, guideline/payment violations',
        ],
        gotchas: ['Review latency means you cannot instantly hotfix — plan with feature flags and staged rollout'],
      },
      {
        name: 'Staged rollout',
        shortLabel: 'rollout',
        description: 'Release to a small percentage of users first, watch health metrics, then ramp to 100% — or halt.',
        durationHint: 'hours to days',
        details: [
          'Start at a few percent, monitor crash-free rate, ANRs, and key metrics, then increase gradually',
          'Halt or roll back the rollout if health regresses before it reaches everyone',
          'Feature flags let you dark-launch and toggle features server-side without a new build',
        ],
      },
      {
        name: 'Monitor & update',
        shortLabel: 'monitor',
        description: 'Watch field crash/ANR rates, performance, and adoption; feed issues into the next release.',
        durationHint: 'continuous',
        details: [
          'Symbolicated crash reports and vitals reveal real-world failures you could not reproduce',
          'Users adopt updates gradually, so old versions live on — keep servers backward compatible',
          'Findings drive the next commit, closing the loop back to build',
        ],
      },
    ],
    keyTakeaways: [
      'Mobile releases are gated by signing, store review, and staged rollout — you cannot instantly deploy or hotfix',
      'Signing key custody is critical: lose it and users cannot update in place',
      'Staged rollout + feature flags are how you de-risk, because a bad build cannot be recalled instantly',
      'Old app versions persist in the wild — keep backends backward compatible',
    ],
    interviewNotes: [
      'Explain why you cannot hotfix a mobile app as fast as a web app and what you do instead',
      'Describe staged rollout and what metrics you monitor to decide whether to ramp or halt',
      'Explain the consequences of losing the app signing key',
    ],
    relatedTopics: ['build-systems-signing', 'app-store-submission', 'release-management', 'mobile-cicd'],
  },
  {
    id: 'permission-request-flow',
    title: 'The Runtime Permission Request Flow',
    subtitle: 'Explain → request → handle grant/deny/revoke, always re-checking',
    icon: '🔐',
    flow: 'cyclic',
    overview:
      'Sensitive capabilities (location, camera, mic, contacts, notifications) require runtime consent the user can grant, deny, or later revoke. Grant rates and trust hinge on asking in context with a clear rationale, and the app must keep working when denied. Because permissions can change while the app is backgrounded, every use of a protected capability must re-check state first — permission is not a one-time gate.',
    stages: [
      {
        name: 'Declare capability',
        shortLabel: 'declare',
        description: 'The app statically declares which protected capabilities it may use.',
        durationHint: 'at build time',
        details: [
          'Declared in the manifest (Android) / Info.plist usage descriptions (iOS)',
          'iOS requires a human-readable purpose string explaining why each capability is needed',
          'Declaring more than you use is a red flag in store review and for privacy-conscious users',
        ],
      },
      {
        name: 'Trigger in context',
        shortLabel: 'in context',
        description: 'Request the permission at the moment the feature that needs it is actually used.',
        durationHint: 'just-in-time',
        details: [
          'Just-in-time requests (tapping "attach photo" → ask for photos) massively outperform launch-time walls',
          'Optionally show a pre-permission rationale screen before the system dialog to set expectations',
          'Never dump all permission dialogs at first launch — users deny what they do not understand',
        ],
        gotchas: ['Front-loading permission prompts at launch tanks grant rates and trust'],
      },
      {
        name: 'System prompt',
        shortLabel: 'prompt',
        description: 'The OS shows the permission dialog; the user chooses the level of access to grant.',
        durationHint: 'user decides',
        details: [
          'Modern grants are granular: approximate vs precise location, one-time, while-using vs always, selected-photos',
          'The OS controls the dialog; you cannot restyle it, only prepare the user beforehand',
          'On many platforms a hard denial means you cannot prompt again — you must route to settings',
        ],
      },
      {
        name: 'Handle the result',
        shortLabel: 'handle',
        description: 'Branch on granted (full or partial), denied, or one-time, and adapt the feature accordingly.',
        durationHint: 'immediate',
        details: [
          'Granted: proceed, but respect partial grants (e.g. approximate-only location or selected photos)',
          'Denied: degrade gracefully — keep the app usable, explain what is unavailable, offer a settings path',
          'One-time grants must be re-requested next session; do not assume persistence',
        ],
        gotchas: ['Crashing or blocking the whole app when a permission is denied is a common, avoidable failure'],
      },
      {
        name: 'Use & re-check (loop back)',
        shortLabel: 're-check',
        description: 'Before each use, re-verify the permission, since the user may have revoked it in settings while away.',
        durationHint: 'every use',
        details: [
          'Permissions can be revoked at any time from system settings, even while the app is backgrounded',
          'Always check current permission state right before using the capability, not just once at grant time',
          'Handle mid-session revocation gracefully — stop the feature and re-explain rather than crashing',
        ],
      },
    ],
    keyTakeaways: [
      'Ask in context with a rationale — just-in-time requests dramatically improve grant rates',
      'Denial and revocation are normal states: degrade gracefully and never crash or nag',
      'Modern permissions are granular (approximate location, selected photos, one-time) — handle partial grants',
      'Re-check permission state before every use because it can be revoked while backgrounded',
    ],
    interviewNotes: [
      'Explain why just-in-time permission requests outperform requesting everything at launch',
      'Describe how you handle a permission being revoked while the app was in the background',
      'Give examples of granular permission levels and how the UI should adapt to each',
    ],
    relatedTopics: ['permission-model', 'location-maps', 'camera-media', 'mobile-privacy'],
  },
]
