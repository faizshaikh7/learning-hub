import type { CurriculumTopic } from '@/types'

/**
 * Senior / CTO-level "advanced" Backend Engineering topics.
 * These extend BACKEND_CURRICULUM with deeper, production-grade material.
 */
export const BACKEND_EXTRA_TOPICS: CurriculumTopic[] = [
  {
    "id": "idempotency-exactly-once",
    "phase": 3,
    "phaseName": "Scale & Performance",
    "orderIndex": 99,
    "estimatedMins": 55,
    "prerequisites": [
      "message-queues",
      "database-transactions"
    ],
    "title": "Idempotency & Exactly-Once Delivery",
    "eli5": "Imagine pressing an elevator button. Pressing it five times does not call five elevators — the button remembers it is already pressed. Idempotency means doing the same thing many times has the same effect as doing it once. That is how a payment button avoids charging you five times when your phone retries a slow request.",
    "analogy": "An idempotency key is like a coat-check ticket. The first time you hand over your coat you get ticket #42 and the coat goes on the rack. If you nervously hand over the same ticket again, the attendant does not take a second coat — they just point at the coat already hanging under #42 and hand you back the same claim. The ticket makes a repeated request return the original result instead of doing the work twice.",
    "explanation": "Networks are unreliable, so clients and message systems retry. Retries mean the same operation can arrive more than once. If that operation has side effects — charging a card, shipping an order, incrementing a balance — duplicates are catastrophic. Idempotency is the property that processing a request N times produces the same result and the same side effects as processing it once. You achieve it by giving each logical operation a stable idempotency key, recording the outcome of the first execution in a dedup store, and short-circuiting any later request that carries the same key to replay the stored result. The hard truth every senior engineer internalizes: true exactly-once *delivery* over a network is impossible (it reduces to the Two Generals Problem). What you can build is exactly-once *processing* — at-least-once delivery plus idempotent, deduplicated handlers.",
    "technicalDeep": "Three delivery semantics: (1) at-most-once — fire and forget, no retries, messages can be lost; (2) at-least-once — retry until acknowledged, so duplicates are guaranteed under failure; (3) exactly-once — the holy grail that cannot be achieved for delivery because acknowledgements can themselves be lost. The proof is the Two Generals Problem: no finite protocol over a lossy channel can guarantee both parties agree a message was received. Real systems pick at-least-once delivery and make the *effect* exactly-once by deduplicating on a business key. Kafka's 'exactly-once semantics' (EOS) is not magic delivery — it is idempotent producers (a producer id + monotonic sequence number the broker uses to drop duplicate appends) plus transactional writes that atomically commit messages and consumer offsets; it only holds inside the Kafka boundary, not across an external side effect like a Stripe call. For HTTP, RFC 9110 defines GET, HEAD, PUT, DELETE, OPTIONS as idempotent and POST/PATCH as generally not — which is exactly why Stripe, PayPal, and every serious payment API layer an `Idempotency-Key` header on POST. The dedup store maps key -> {status, response_snapshot, request_fingerprint} with a TTL (Stripe keeps keys 24h). The store must be strongly consistent and support atomic put-if-absent (Redis `SET key val NX PX ttl`, or a unique constraint in Postgres) so two concurrent retries cannot both 'win'. For local database work, the durable pattern is the transactional outbox: write your business row and an outbox row in the same DB transaction, then a relay publishes the outbox row to the broker at-least-once — this eliminates the dual-write problem where a crash between 'commit to DB' and 'publish to queue' loses the event.",
    "whatBreaks": "The classic failure: your handler does the work, then crashes before writing the dedup record — the retry re-does the work. Fix: make the side effect and the dedup record commit atomically, or make the side effect itself idempotent (upsert, not insert). Another: using a client-generated key that is not actually unique per logical operation (e.g. reusing a key across a retry with a *different* body) — Stripe returns 400 if the same key arrives with a mismatched request fingerprint, which is a feature, not a bug. Non-atomic check-then-act (`GET key; if absent, do work`) is a race: two retries both read 'absent' and both charge. TTLs that are too short let a slow retry outlive the key and double-process. Naive Kafka consumers that commit offsets before processing turn at-least-once into at-most-once and silently drop data on crash. And 'exactly-once' claims that span an external system (queue + third-party API) are always false — you still need idempotency on the far side.",
    "efficientWay": {
      "title": "Achieving Exactly-Once Effects",
      "approaches": [
        {
          "name": "At-least-once delivery + idempotent handler with a dedup key",
          "verdict": "best",
          "reason": "The only approach that survives real network and crash failures. Retries are safe because duplicates collapse to one effect. This is what Stripe, Kafka EOS, and every payment rail actually do."
        },
        {
          "name": "Trust the broker's 'exactly-once' flag and skip dedup in your handler",
          "verdict": "weak",
          "reason": "Broker EOS only holds inside the broker's boundary. The moment your handler calls an external API or another DB, you are back to at-least-once and need your own idempotency."
        },
        {
          "name": "At-most-once (no retries) to avoid duplicates",
          "verdict": "weak",
          "reason": "You trade duplicates for silent data loss. Almost never acceptable for money, orders, or state changes."
        },
        {
          "name": "Dual-write: commit to DB then publish to queue in app code",
          "verdict": "ok",
          "reason": "Works until a crash lands between the two writes and you lose the event. Fine only for non-critical, replayable data; use the transactional outbox for anything that matters."
        }
      ],
      "recommendation": "Assume at-least-once everywhere. Give every state-changing operation a stable idempotency key, store the first outcome atomically with the side effect (or make the side effect an upsert), and use the transactional outbox pattern to bridge DB and message bus. Never claim exactly-once across a system boundary — claim exactly-once *processing* backed by dedup."
    },
    "commonMistakes": [
      "Believing a message broker gives you exactly-once delivery end-to-end — it only gives you at-least-once plus tools to build idempotency",
      "Non-atomic check-then-act on the dedup store, creating a race where two concurrent retries both perform the side effect",
      "Doing the work first and recording the idempotency key second, so a crash in between causes a double-charge on retry",
      "Reusing an idempotency key for a logically different request, or generating a new key on each retry (defeating the whole mechanism)",
      "Committing Kafka/SQS offsets before the message is fully processed, turning at-least-once into silent at-most-once data loss",
      "Setting the dedup TTL shorter than the maximum realistic retry window",
      "Dual-writing to DB and queue from application code and losing events when the process dies between the two writes"
    ],
    "seniorNotes": "The sentence that separates senior from mid in an interview: 'Exactly-once delivery is impossible over an unreliable network; we engineer exactly-once processing via at-least-once delivery plus idempotent, deduplicated handlers.' In design reviews, always ask 'what is the idempotency key and where is the dedup boundary?' for every write path. Prefer natural idempotency (upserts keyed on a business identifier) over bolted-on dedup tables when you can, because it removes a moving part. For money movement, the idempotency key should be generated by the *client* at the moment of intent and persisted, so that even a full client restart replays the same key. The transactional outbox plus a change-data-capture relay (Debezium) is the current best-practice answer to the dual-write problem and shows up constantly in senior system-design rounds.",
    "interviewQuestions": [
      "Why is exactly-once delivery impossible, and what do we build instead?",
      "Design an idempotent payment charge endpoint. Where does the idempotency key come from and what does the dedup store hold?",
      "Which HTTP methods are idempotent, and why is POST the odd one out for payments?",
      "A consumer reads from a queue at-least-once. Walk me through making the handler safe against duplicate delivery.",
      "What is the dual-write problem and how does the transactional outbox pattern solve it?",
      "Kafka advertises 'exactly-once semantics.' What does that actually guarantee and where does the guarantee stop?"
    ],
    "interviewAnswers": [
      "Exactly-once *delivery* requires both sender and receiver to agree that a message arrived, over a channel that can drop any message including the acknowledgement — that is the Two Generals Problem, provably unsolvable with a finite protocol. So we choose at-least-once delivery (retry until acked, accepting duplicates) and make the *effect* exactly-once by deduplicating on a stable business key. The correct phrase is 'exactly-once processing,' not delivery.",
      "The client generates an idempotency key at the moment of intent (a UUID persisted before the first send) and passes it in an `Idempotency-Key` header. The server does an atomic put-if-absent into a strongly-consistent dedup store keyed by (merchant, key). If it wins, it performs the charge and stores the response plus a fingerprint of the request body under that key with a TTL (Stripe uses 24h). If the key already exists, it replays the stored response instead of charging again — and returns 409/400 if the same key arrives with a different body. The charge and the dedup record should commit together, or the charge must itself be an upsert keyed on the idempotency key.",
      "GET, HEAD, PUT, DELETE, and OPTIONS are idempotent by the HTTP spec — repeating them lands the resource in the same state. POST is not, because it means 'create/append,' so a retried POST creates a second charge. That is precisely why payment APIs add an idempotency key to POST: it retrofits idempotency onto an inherently non-idempotent verb so a retried request returns the original result rather than charging twice.",
      "Assume every message can arrive more than once. Before performing the side effect, atomically claim the message's business key in a dedup store (unique constraint or `SET NX`). If the claim fails, the message is a duplicate — ack and drop it. If it succeeds, perform the side effect and record its outcome in the same transaction, then ack. Crucially, ack only *after* processing so a crash re-delivers rather than loses the message, and make the side effect an upsert where possible so even a duplicate that slips through is harmless.",
      "The dual-write problem is needing to update your database and publish an event to a broker as one atomic unit, when they are two separate systems with no shared transaction — a crash between them either loses the event or emits an event for a DB change that rolled back. The transactional outbox solves it by writing the business row and an `outbox` row in the *same* DB transaction. A separate relay (polling or change-data-capture like Debezium) reads committed outbox rows and publishes them to the broker at-least-once, marking them sent. The DB is the single source of truth, so the event and the state change can never disagree.",
      "Kafka EOS guarantees that, *within Kafka*, a producer's retries do not create duplicate records (idempotent producer via producer-id + sequence number) and that a batch of messages plus the consumer offset commit are written atomically (transactions). So a Kafka-to-Kafka stream-processing pipeline can be exactly-once. The guarantee stops at Kafka's boundary: the instant your consumer performs an external side effect — a Stripe charge, a write to another database, an email — you are back to at-least-once and must add your own idempotency key on that side effect."
    ],
    "codeExamples": [
      {
        "lang": "typescript",
        "label": "Idempotency-key middleware (Express + Redis, atomic claim)",
        "code": "import type { Request, Response, NextFunction } from 'express'\nimport { createHash } from 'node:crypto'\nimport type { Redis } from 'ioredis'\n\n// Redis holds: idem:{merchant}:{key} -> JSON({ status, fingerprint, response })\n// TTL should exceed the longest realistic client retry window.\nconst IDEM_TTL_SECONDS = 24 * 60 * 60 // Stripe-style 24h\n\nexport function idempotency(redis: Redis) {\n  return async (req: Request, res: Response, next: NextFunction) => {\n    const key = req.header('Idempotency-Key')\n    if (!key) return next() // only enforce on writes that opt in\n\n    const merchant = (req as any).auth?.merchantId ?? 'anon'\n    const storeKey = `idem:${merchant}:${key}`\n    const fingerprint = createHash('sha256')\n      .update(req.method + req.originalUrl + JSON.stringify(req.body ?? {}))\n      .digest('hex')\n\n    // Atomic claim: only the first request wins. NX = set if not exists.\n    const claimed = await redis.set(\n      storeKey,\n      JSON.stringify({ status: 'in_progress', fingerprint }),\n      'EX', IDEM_TTL_SECONDS, 'NX',\n    )\n\n    if (!claimed) {\n      // Key already exists: this is a retry (or a conflicting reuse).\n      const raw = await redis.get(storeKey)\n      const record = raw ? JSON.parse(raw) : null\n      if (record?.fingerprint && record.fingerprint !== fingerprint) {\n        // Same key, different body -> caller bug. Never re-execute.\n        return res.status(422).json({ error: 'idempotency_key_reused_with_different_body' })\n      }\n      if (record?.status === 'completed') {\n        return res.status(record.response.status).json(record.response.body)\n      }\n      // First request still running: tell the client to retry shortly.\n      return res.status(409).json({ error: 'request_in_progress' })\n    }\n\n    // We own the key. Capture the response so retries can replay it.\n    const originalJson = res.json.bind(res)\n    res.json = (body: unknown) => {\n      void redis.set(\n        storeKey,\n        JSON.stringify({ status: 'completed', fingerprint, response: { status: res.statusCode, body } }),\n        'EX', IDEM_TTL_SECONDS,\n      )\n      return originalJson(body)\n    }\n    next()\n  }\n}"
      },
      {
        "lang": "typescript",
        "label": "Transactional outbox: atomic write + at-least-once relay",
        "code": "// 1) SCHEMA\n// CREATE TABLE orders (id uuid PRIMARY KEY, status text, total_cents int);\n// CREATE TABLE outbox (\n//   id           bigserial PRIMARY KEY,\n//   aggregate_id uuid NOT NULL,\n//   event_type   text NOT NULL,\n//   payload      jsonb NOT NULL,\n//   created_at   timestamptz NOT NULL DEFAULT now(),\n//   published_at timestamptz              -- NULL until relayed\n// );\n\nimport type { Pool, PoolClient } from 'pg'\n\n// Write the business row AND the event in ONE transaction.\n// No dual-write: if the tx commits, both exist; if it aborts, neither does.\nexport async function placeOrder(pool: Pool, order: { id: string; totalCents: number }) {\n  const client: PoolClient = await pool.connect()\n  try {\n    await client.query('BEGIN')\n    await client.query(\n      'INSERT INTO orders (id, status, total_cents) VALUES ($1, $2, $3)',\n      [order.id, 'placed', order.totalCents],\n    )\n    await client.query(\n      `INSERT INTO outbox (aggregate_id, event_type, payload)\n       VALUES ($1, $2, $3)`,\n      [order.id, 'order.placed', JSON.stringify(order)],\n    )\n    await client.query('COMMIT')\n  } catch (err) {\n    await client.query('ROLLBACK')\n    throw err\n  } finally {\n    client.release()\n  }\n}\n\n// Relay: publishes committed events at-least-once, then marks them sent.\n// Consumers MUST dedup on event id because a crash after publish, before\n// UPDATE, will re-publish. FOR UPDATE SKIP LOCKED lets many relays run.\nexport async function relayOutbox(pool: Pool, publish: (e: unknown) => Promise<void>) {\n  const client = await pool.connect()\n  try {\n    await client.query('BEGIN')\n    const { rows } = await client.query(\n      `SELECT id, aggregate_id, event_type, payload\n         FROM outbox\n        WHERE published_at IS NULL\n        ORDER BY id\n        LIMIT 100\n        FOR UPDATE SKIP LOCKED`,\n    )\n    for (const row of rows) {\n      await publish({ id: row.id, type: row.event_type, data: row.payload })\n      await client.query('UPDATE outbox SET published_at = now() WHERE id = $1', [row.id])\n    }\n    await client.query('COMMIT')\n  } catch (err) {\n    await client.query('ROLLBACK')\n    throw err\n  } finally {\n    client.release()\n  }\n}"
      }
    ]
  },
  {
    "id": "pagination-patterns",
    "phase": 1,
    "phaseName": "Backend Foundations",
    "orderIndex": 99,
    "estimatedMins": 40,
    "prerequisites": [
      "rest-api-design",
      "database-indexing"
    ],
    "title": "API Pagination: Offset vs Cursor",
    "eli5": "Reading a huge list of results is like reading a giant book. Offset pagination is saying 'skip the first 2,000 pages and read one page' — the reader still has to physically flip past all 2,000 pages every time, so deep pages get slower and slower. Cursor pagination is using a bookmark: 'start right after where I left off.' The reader jumps straight to the bookmark no matter how deep it is.",
    "analogy": "Offset pagination is counting people in a moving line by index: 'give me people 100 through 110.' If someone cuts in near the front, everyone shifts and you either re-see or skip a person. Cursor pagination is 'give me the next 10 people standing after Alice' — even if the front of the line churns, 'after Alice' still points to the same place, so you never double-count or miss anyone.",
    "explanation": "Pagination splits a large result set into pages so clients fetch data in bounded chunks. The two dominant styles are offset-based (`LIMIT n OFFSET m` / `?page=3&size=20`) and cursor/keyset-based (`WHERE (sort_key) > :cursor ORDER BY sort_key LIMIT n`). Offset is trivial to implement and lets users jump to arbitrary pages, but it has two serious flaws at scale: it gets slower the deeper you go (the database must scan and discard every skipped row), and it is unstable under concurrent inserts and deletes (rows shift between requests, causing duplicates or gaps). Cursor pagination encodes 'where you left off' as an opaque token derived from the last row's sort key(s). It stays fast at any depth because it seeks directly into an index, and it is stable because the anchor is a value, not a position. The tradeoff: cursors only support next/previous traversal, not random page jumps, and they require a stable, unique, indexed sort order.",
    "technicalDeep": "Offset cost: `LIMIT 20 OFFSET 100000` forces the engine to read and throw away 100,000 rows before returning 20 — an O(offset) scan that turns page 5,000 into a full-table-ish read. Keyset pagination replaces the offset with a range predicate on the sort columns: `WHERE (created_at, id) < (:last_created_at, :last_id) ORDER BY created_at DESC, id DESC LIMIT 21`. With a composite index on `(created_at, id)` this is an O(log n) index seek plus a sequential scan of exactly the page — constant time regardless of depth. The `id` tiebreaker is mandatory: sorting only on a non-unique column (like `created_at`) makes the boundary ambiguous, so you must append a unique column to make the total order deterministic; otherwise rows sharing a timestamp straddle the page boundary and get skipped or duplicated. Cursors are typically the base64 of the tuple of sort values (an opaque page token), sometimes signed/encrypted so clients cannot forge or depend on the internals — this is what Stripe, GitHub, Slack, and Twitter/X APIs expose. Fetch `n+1` rows to cheaply compute `hasNextPage` without a second query. On total counts: `SELECT COUNT(*)` over a large filtered set is itself an expensive scan, so senior APIs either omit total count, return an approximate count (Postgres `reltuples` estimate or a maintained counter), or compute it only on the first page. Sorting stability is the crux: the sort must be a strict total order (unique) and, ideally, on immutable columns — paginating by a mutable field (like `updated_at` or a like-count) means a row can move between pages mid-traversal.",
    "whatBreaks": "Deep offset pages time out or hammer CPU because every request rescans all skipped rows — a common cause of a slow endpoint that 'works fine on page 1.' Under inserts, offset pagination shows the same record twice (a new row shifts everything down between page 1 and page 2) or, under deletes, silently skips records — classic bugs in infinite-scroll feeds. Cursor pagination breaks if the sort key is not unique: rows with identical timestamps land on a page boundary and vanish or repeat — always add a unique tiebreaker. It also breaks if you change the sort order without invalidating outstanding cursors, or if clients try to reverse-engineer the token. `COUNT(*)` on every page can dominate latency on big tables. And keyset with `OFFSET` bolted on (to fake page jumps) reintroduces every offset problem.",
    "efficientWay": {
      "title": "Choosing a Pagination Strategy",
      "approaches": [
        {
          "name": "Keyset/cursor pagination on a unique, indexed, immutable sort key",
          "verdict": "best",
          "reason": "Constant-time at any depth and stable under concurrent writes. The correct default for feeds, timelines, exports, and any list that can grow large or churn."
        },
        {
          "name": "Offset/limit pagination",
          "verdict": "ok",
          "reason": "Fine for small, bounded, admin-style tables where users genuinely need to jump to page N and the data barely changes. Degrades and destabilizes on large or active datasets."
        },
        {
          "name": "Offset pagination with a live COUNT(*) on every request",
          "verdict": "weak",
          "reason": "You pay the deep-scan cost twice — once for the page, once for the count. On big tables the count alone can dominate response time."
        },
        {
          "name": "Cursor pagination sorted only on a non-unique column (e.g. created_at)",
          "verdict": "weak",
          "reason": "Ambiguous page boundaries drop or duplicate rows that share the sort value. Always append a unique tiebreaker to form a strict total order."
        }
      ],
      "recommendation": "Default to keyset pagination with a composite `(sort_col, unique_id)` index and an opaque page token. Fetch n+1 rows for `hasNextPage`. Only reach for offset when users must jump to arbitrary pages on a small, stable table, and in that case skip or approximate the total count."
    },
    "commonMistakes": [
      "Using offset pagination on a large, actively-written table and being surprised that deep pages are slow and show duplicates",
      "Cursor-sorting on a non-unique column without a unique tiebreaker, causing rows on the boundary to be skipped or repeated",
      "Running SELECT COUNT(*) on every page over a huge filtered set instead of omitting or approximating the total",
      "Exposing raw offsets or decodable cursors that clients then hardcode, locking you out of changing the sort implementation",
      "Paginating by a mutable field (updated_at, score) so records jump between pages while a client is scrolling",
      "Forgetting the index must match the exact sort order (column order and direction), so the 'keyset' query still does a full sort"
    ],
    "seniorNotes": "In an API design review, the reflex answer is 'keyset by default, offset only for small admin tables.' The two follow-ups interviewers love: 'why is the unique tiebreaker mandatory?' (to guarantee a strict total order so boundaries are deterministic) and 'how do you give the client a total count without killing the DB?' (omit it, approximate via `reltuples`/a maintained counter, or count only on page one). Make cursors opaque and ideally signed so they are an implementation detail you can evolve. Note that GraphQL's Relay Connection spec standardizes exactly this model — `edges`, `cursor`, `pageInfo.hasNextPage` — which is why it maps cleanly onto keyset pagination. For bi-directional scroll, store both the leading and trailing row keys. And remember pagination correctness is a consistency question: under snapshot isolation a single long scan is stable, but across separate paginated requests only keyset gives you insert/delete stability.",
    "interviewQuestions": [
      "Why does `LIMIT 20 OFFSET 100000` get slower as the offset grows, and what does keyset pagination do instead?",
      "A user reports seeing the same item twice while scrolling a feed. What is the likely pagination bug and how do you fix it?",
      "Why must a cursor's sort key include a unique tiebreaker?",
      "How would you return a total result count without running an expensive COUNT(*) on every page?",
      "What are the tradeoffs that make offset pagination still acceptable in some cases?",
      "Design the SQL and the page-token format for a cursor-paginated 'recent activity' endpoint."
    ],
    "interviewAnswers": [
      "`OFFSET 100000` forces the database to generate and discard the first 100,000 rows before it can return the next 20 — the work is O(offset), so latency grows linearly with page depth and page 5,000 can scan nearly the whole table. Keyset pagination replaces the positional skip with a value predicate: `WHERE (created_at, id) < (:last_created_at, :last_id) ORDER BY created_at DESC, id DESC LIMIT 21`. Backed by a composite index on `(created_at, id)`, that is an index seek to the anchor plus a scan of just the page — constant time regardless of depth.",
      "It is almost certainly offset pagination under concurrent inserts. Between fetching page 1 and page 2, a new row was inserted near the top, which shifts every row down by one; the row that was last on page 1 is now the first row of page 2, so the client sees it twice (deletes cause the mirror-image bug of skipped rows). The fix is keyset pagination: anchor the next page on the last row's `(sort_key, id)` value instead of a numeric position, so inserts and deletes elsewhere in the list do not shift the anchor.",
      "Because pagination requires a strict total order to define an unambiguous 'next row.' If you sort only on `created_at` and several rows share the same timestamp, the boundary between pages falls in the middle of that tie group — the engine has no deterministic way to say which tied rows already went out, so some get skipped and others repeated across the boundary. Appending a unique column like the primary key (`ORDER BY created_at, id`) makes every row's sort position unique, so the cursor points to exactly one place.",
      "Avoid counting on every page. Options in order of preference: omit the total entirely and expose only `hasNextPage` (fetch n+1 rows to detect it); return an approximate count from the planner statistics (Postgres `reltuples` from `pg_class`) which is instant; maintain a denormalized counter updated by triggers or the write path for an exact-but-cheap number; or compute the exact `COUNT(*)` only on the first page and cache it. Real APIs like GitHub and Twitter simply do not give you a precise total for large collections.",
      "Offset is fine when the dataset is small and bounded, the data changes rarely, and users genuinely need random access to page N — think an internal admin table of a few thousand rows with a page-number UI. There, the deep-scan cost never materializes and the instability window is negligible, so the simplicity of `LIMIT/OFFSET` wins. The moment the table is large, hot with writes, or fronts an infinite-scroll UI, you switch to keyset.",
      "SQL: `SELECT id, actor, verb, created_at FROM activity WHERE (created_at, id) < (:c_created_at, :c_id) ORDER BY created_at DESC, id DESC LIMIT :n + 1` with a composite index on `(created_at DESC, id DESC)`. Fetch n+1 rows; if you got n+1, there is a next page and you drop the extra row. The page token is the base64 (ideally HMAC-signed) encoding of the last returned row's `{created_at, id}` tuple, returned as an opaque `nextCursor`. The client passes it back verbatim; the server decodes it into `:c_created_at` and `:c_id`."
    ],
    "codeExamples": [
      {
        "lang": "sql",
        "label": "Keyset pagination query + supporting index",
        "code": "-- Composite index MUST match the ORDER BY (columns and direction)\n-- so the database seeks instead of sorting.\nCREATE INDEX idx_activity_keyset\n  ON activity (created_at DESC, id DESC);\n\n-- FIRST PAGE: no cursor yet.\nSELECT id, actor_id, verb, created_at\nFROM activity\nWHERE user_id = $1\nORDER BY created_at DESC, id DESC\nLIMIT 21;              -- n + 1 to detect hasNextPage\n\n-- NEXT PAGE: anchor on the last row of the previous page.\n-- Row-value comparison (created_at, id) < (:c_created_at, :c_id)\n-- is the strict-total-order predicate that makes boundaries exact.\nSELECT id, actor_id, verb, created_at\nFROM activity\nWHERE user_id = $1\n  AND (created_at, id) < ($2, $3)   -- ($2,$3) decoded from the cursor\nORDER BY created_at DESC, id DESC\nLIMIT 21;\n\n-- Anti-pattern for contrast: O(offset) deep scan + unstable under writes.\n-- SELECT ... ORDER BY created_at DESC LIMIT 20 OFFSET 100000;"
      },
      {
        "lang": "typescript",
        "label": "Cursor-paginated endpoint with opaque, signed page tokens",
        "code": "import { createHmac } from 'node:crypto'\nimport type { Pool } from 'pg'\n\nconst SECRET = process.env.CURSOR_SECRET!\n\ntype Cursor = { createdAt: string; id: string }\n\n// Encode as an opaque, tamper-evident token so clients cannot forge\n// or depend on the internal shape (we can change it later).\nfunction encodeCursor(c: Cursor): string {\n  const body = Buffer.from(JSON.stringify(c)).toString('base64url')\n  const sig = createHmac('sha256', SECRET).update(body).digest('base64url')\n  return `${body}.${sig}`\n}\n\nfunction decodeCursor(token: string): Cursor {\n  const [body, sig] = token.split('.')\n  const expected = createHmac('sha256', SECRET).update(body).digest('base64url')\n  if (sig !== expected) throw new Error('invalid_cursor')\n  return JSON.parse(Buffer.from(body, 'base64url').toString())\n}\n\nexport async function listActivity(\n  pool: Pool,\n  userId: string,\n  opts: { limit?: number; cursor?: string },\n) {\n  const limit = Math.min(opts.limit ?? 20, 100)\n  const params: unknown[] = [userId]\n  let where = 'user_id = $1'\n\n  if (opts.cursor) {\n    const { createdAt, id } = decodeCursor(opts.cursor)\n    params.push(createdAt, id)\n    where += ` AND (created_at, id) < ($2, $3)`\n  }\n\n  // Fetch limit + 1 to know if another page exists, without COUNT(*).\n  const { rows } = await pool.query(\n    `SELECT id, actor_id, verb, created_at\n       FROM activity\n      WHERE ${where}\n      ORDER BY created_at DESC, id DESC\n      LIMIT ${limit + 1}`,\n    params,\n  )\n\n  const hasNextPage = rows.length > limit\n  const page = hasNextPage ? rows.slice(0, limit) : rows\n  const last = page[page.length - 1]\n\n  return {\n    data: page,\n    pageInfo: {\n      hasNextPage,\n      nextCursor: hasNextPage && last\n        ? encodeCursor({ createdAt: last.created_at.toISOString(), id: last.id })\n        : null,\n    },\n    // Note: no `total` — we deliberately avoid a per-page COUNT(*).\n  }\n}"
      }
    ]
  },
  {
    "id": "api-versioning",
    "phase": 5,
    "phaseName": "Architecture Patterns",
    "orderIndex": 99,
    "estimatedMins": 50,
    "prerequisites": [
      "rest-api-design",
      "microservices-architecture"
    ],
    "title": "API Versioning Strategies",
    "eli5": "An API is a promise to everyone using it. If you suddenly change the promise, all their apps break. Versioning is like keeping the old menu available while you print a new one — customers who memorized the old menu can still order from it, and new customers use the new one. You give people time to switch before you take the old menu away.",
    "analogy": "Versioning an API is like changing the electrical sockets in a country. You cannot rip out every old socket overnight — millions of devices are plugged in. So you introduce the new socket, sell adapters (compatibility shims), announce a switch-off date years ahead (deprecation and sunset), and only then remove the old one. The people who plugged in never had to change everything at once.",
    "explanation": "An API is a contract with consumers you often cannot force to upgrade — mobile apps in users' pockets, partner integrations, third-party scripts. Versioning is how you evolve that contract without breaking existing clients. There are three mechanical places to put a version: in the URI (`/v2/orders`), in a request header (`API-Version: 2`), or in the media type via content negotiation (`Accept: application/vnd.acme.v2+json`). Orthogonal to *where* the version lives is *how* you number it: a small integer or SemVer-style major version, or a date (Stripe's `2024-06-20` style). The deeper senior skill is not picking a scheme — it is minimizing how often you need a new version at all through backward-compatible evolution (the expand/contract or 'parallel change' pattern): add new fields and endpoints additively, never repurpose or remove in place, and only cut a new major version for genuinely breaking changes. When you must break, you need a real deprecation policy: announce, emit `Deprecation` and `Sunset` headers (RFC 8594), give consumers a long window, and provide migration guides and shims.",
    "technicalDeep": "URI versioning (`/v1/...`) is the most visible and cache/router-friendly — every proxy, log, and browser sees the version — but it is the least RESTful (the same resource now has two URLs) and encourages coarse, whole-API version bumps. Header versioning (`API-Version: 2024-06-20`) keeps URLs clean and stable and lets you version at fine granularity, at the cost of being invisible in a browser address bar and harder to test with a plain link; it also requires disciplined `Vary` handling so caches key on the version. Media-type versioning (`Accept: application/vnd.acme.v2+json`) is the purist's content-negotiation answer — the resource URL never changes, you negotiate the representation — but tooling support and developer ergonomics are weakest. Date-based versioning is what Stripe popularized: each account is pinned to the API version in effect when it started, requests can override per-call with a `Stripe-Version` header, and internally Stripe runs an ordered chain of *version-change transformers* that walk a modern response backward through every intermediate version's shim until it matches the caller's pinned date. This is powerful because the core code always speaks the newest schema and compatibility lives in isolated, testable transform functions. Semantic versioning maps to APIs as: MAJOR = breaking change (removed/renamed field, changed type or semantics), MINOR = backward-compatible addition, PATCH = backward-compatible fix — but on the wire consumers usually only select the MAJOR. Expand/contract (parallel change) is the migration mechanic: EXPAND = add the new field/endpoint alongside the old and dual-write; migrate consumers; CONTRACT = remove the old only after telemetry shows no one uses it. Deprecation is signaled with the `Deprecation` header (a date or `true`) and the `Sunset` header (RFC 8594, the removal timestamp), plus `Link` headers pointing at the migration doc; you monitor per-version, per-consumer usage so the sunset date is data-driven, not hopeful.",
    "whatBreaks": "The number-one production incident is a 'small' change that was actually breaking: renaming a JSON field, tightening validation, changing a default, narrowing a type, or making an optional field required — all silently break clients that assumed the old shape. Versioning in the URL but treating `/v1` and `/v2` as forks means you now maintain and bug-fix two codebases forever, and they drift. Header/media-type versioning without correct `Vary` headers poisons shared caches, serving a v2 body to a v1 client. Deprecating without telemetry means you either never dare remove the old version (permanent maintenance tax) or you remove it and take down a partner who was still calling it. Sunsetting with too short a window strands mobile clients that users have not updated. And 'just bump the major version' for every change explodes into a combinatorial matrix of versions no team can test.",
    "efficientWay": {
      "title": "Choosing and Running a Versioning Strategy",
      "approaches": [
        {
          "name": "Backward-compatible additive evolution (expand/contract) as the default, breaking changes as the rare exception",
          "verdict": "best",
          "reason": "The cheapest version is the one you never cut. Additive changes keep one codebase and every existing client working; you only pay versioning cost for true breaks."
        },
        {
          "name": "Date-based versioning with per-account pinning and transformer shims (Stripe style)",
          "verdict": "best",
          "reason": "Lets the core code always speak the latest schema while isolated, testable transforms adapt responses to each pinned version. Scales to years of backward compatibility without forking the codebase."
        },
        {
          "name": "URI major versioning (/v1, /v2)",
          "verdict": "ok",
          "reason": "Simple, explicit, cache- and router-friendly, great for public APIs. But it invites coarse whole-API bumps and, if implemented as forked code, becomes a permanent multi-codebase maintenance burden."
        },
        {
          "name": "Forking the codebase per version, or bumping the major version for every change",
          "verdict": "weak",
          "reason": "Forks drift and multiply bug-fix work; per-change majors explode into an untestable version matrix. Both signal a missing backward-compatibility discipline."
        }
      ],
      "recommendation": "Optimize to *not* version: evolve additively with expand/contract and treat the wire schema as append-only. When you truly must break, prefer a single well-known scheme (URI major for broad public APIs, date-based pinning for high-compatibility platforms), keep the core on the latest schema with transformer shims, and back every deprecation with `Deprecation`/`Sunset` headers, per-consumer telemetry, and a generous migration window."
    },
    "commonMistakes": [
      "Shipping a 'minor' change that is actually breaking — renaming a field, tightening validation, or making an optional field required",
      "Implementing URI versions as forked codebases that drift and double every future bug fix",
      "Using header or media-type versioning without correct `Vary` headers, poisoning shared caches across versions",
      "Deprecating an endpoint with no usage telemetry, so you either never remove it or remove it and break a live consumer",
      "Setting a sunset window too short for mobile clients that users are slow to update",
      "Cutting a brand-new major version for every change instead of evolving additively, creating an unmaintainable version matrix",
      "Never communicating deprecation via `Deprecation`/`Sunset` headers and migration docs, so consumers are blindsided"
    ],
    "seniorNotes": "The line that lands in a design review: 'The best version is the one we never have to cut — we evolve additively and reserve versioning for real breaking changes.' Interviewers probe two things: can you name what actually constitutes a breaking change (removing/renaming a field, changing a type or default, tightening validation, changing error semantics) versus a safe additive one, and do you have a *retirement* story, not just an introduction story. A senior answer always includes telemetry-driven deprecation: instrument usage per version and per consumer so the sunset date is defensible, emit RFC 8594 `Sunset` headers, and provide shims. Stripe's transformer architecture is the canonical case study — mention that compatibility living in isolated version-change modules keeps the core clean and each shim independently testable. For internal microservices you can be more aggressive (consumer-driven contract tests + coordinated rollouts) than for public APIs where you cannot force upgrades. Also note protocol context: gRPC/protobuf and Avro bake backward/forward compatibility into field numbering and schema-registry rules, which is 'versioning' by making the wire format tolerant rather than by numbering endpoints.",
    "interviewQuestions": [
      "What actually counts as a breaking change to an API, and what changes are safe to ship without a new version?",
      "Compare URI, header, and media-type versioning. When would you pick each?",
      "Explain Stripe's date-based versioning. How can their core code always speak the latest schema while old clients keep working?",
      "What is the expand/contract (parallel change) pattern and how do you use it to remove a field safely?",
      "How do you decide when it is safe to actually shut off an old API version?",
      "Which HTTP headers signal deprecation and sunset, and what belongs in a good deprecation policy?"
    ],
    "interviewAnswers": [
      "A breaking change is anything that can make a previously-valid client stop working: removing or renaming a field or endpoint, changing a field's type or its semantic meaning, changing a default value, tightening validation, making an optional request field required, or changing error codes/shapes clients branch on. Safe additive changes are the opposite: adding a new optional field to a response, adding a new endpoint, adding a new optional request parameter with a backward-compatible default, or adding a new enum value *only if* clients were told to tolerate unknown values. The rule of thumb: you may add, you must not remove or repurpose in place.",
      "URI versioning (`/v2/...`) is explicit, visible in logs and browsers, and trivially cache- and router-friendly, so it is the pragmatic default for broad public APIs — but it is un-RESTful (two URLs for one resource) and nudges you toward coarse whole-API bumps. Header versioning (`API-Version: ...`) keeps URLs clean and lets you version at fine granularity, good for platforms with many small evolutions, but it is invisible in a plain link and needs careful `Vary`/cache handling. Media-type versioning (`Accept: application/vnd.acme.v2+json`) is the most RESTful — the URL is stable and you content-negotiate the representation — but tooling and developer ergonomics are weakest, so it is rare outside hypermedia-purist APIs.",
      "Stripe pins each account to the API version that was current when it signed up, and a request can override per-call with the `Stripe-Version` header. Internally the core code is written against only the newest schema. Compatibility lives in an ordered chain of version-change transformers — small, isolated functions that each know how to downgrade a response from one version to the previous one. To serve an old client, Stripe generates the modern response and runs it backward through every transformer between today and the client's pinned date until the shape matches what that client expects. The payoff: the core stays clean and modern, and each compatibility shim is small and independently testable.",
      "Expand/contract (a.k.a. parallel change) removes or changes something in three safe phases. EXPAND: introduce the new field/endpoint alongside the old one and dual-write, so both the old and new shapes are valid and populated simultaneously. MIGRATE: move consumers to the new shape and instrument usage of the old one. CONTRACT: once telemetry shows the old field has zero (or acceptably low, sunset-eligible) traffic, remove it. This lets you make a logically breaking change without a flag-day cutover, because at no single moment are old and new consumers both broken.",
      "You make it a data-driven decision, never a calendar guess. Instrument the old version by consumer — API key, account, user-agent — so you can see exactly who still calls it and how often. Announce the deprecation early, emit `Deprecation` and `Sunset` headers on every response from the old version, publish a migration guide, and proactively reach out to the heaviest remaining callers. Only schedule the actual shutoff once usage has decayed to zero or to a known, contacted set of stragglers, and give mobile clients an extra-long window because users update slowly. The sunset date should be defensible with a usage graph.",
      "RFC 8594 defines the `Sunset` header — an HTTP-date marking when the resource/version will stop working — and the `Deprecation` header signals that a resource is deprecated (a date or `true`), often paired with `Link` headers pointing at the successor and the migration doc. A good deprecation policy bundles these signals with: an early, clearly-communicated timeline; per-consumer usage telemetry to know who is affected; documented migration paths and ideally compatibility shims; a generous window sized to your slowest clients (mobile); and direct outreach to the biggest remaining consumers before the sunset date arrives."
    ],
    "codeExamples": [
      {
        "lang": "typescript",
        "label": "Version routing: URI + header, resolving to a single canonical handler",
        "code": "import express, { type Request, type Response, type NextFunction } from 'express'\n\nconst app = express()\n\n// Supported API versions, newest last. Date-based (Stripe style).\nconst VERSIONS = ['2023-01-01', '2024-06-20', '2025-03-15'] as const\ntype ApiVersion = (typeof VERSIONS)[number]\nconst LATEST: ApiVersion = VERSIONS[VERSIONS.length - 1]\n\n// Resolve the effective version from (1) explicit header, else\n// (2) the account's pinned version, else (3) the latest.\nfunction resolveVersion(req: Request): ApiVersion {\n  const header = req.header('Api-Version') as ApiVersion | undefined\n  if (header && VERSIONS.includes(header)) return header\n  const pinned = (req as any).account?.pinnedApiVersion as ApiVersion | undefined\n  return pinned ?? LATEST\n}\n\napp.use((req, res, next) => {\n  const version = resolveVersion(req)\n  ;(req as any).apiVersion = version\n  // Cache correctness: responses vary by the version header.\n  res.setHeader('Vary', 'Api-Version')\n  res.setHeader('Api-Version', version)\n\n  // Announce deprecation of everything older than the latest major.\n  if (version < '2024-06-20') {\n    res.setHeader('Deprecation', 'true')\n    res.setHeader('Sunset', 'Wed, 31 Dec 2025 23:59:59 GMT') // RFC 8594\n    res.setHeader('Link', '<https://api.acme.com/docs/migrate>; rel=\"deprecation\"')\n  }\n  next()\n})\n\n// ONE canonical handler speaks the LATEST schema; shims adapt on the way out.\napp.get('/orders/:id', async (req: Request, res: Response) => {\n  const order = await getOrder(req.params.id) // always newest shape\n  const body = applyVersionShims(order, (req as any).apiVersion)\n  res.json(body)\n})\n\n// Optional explicit URI-major routing that maps onto the same core.\n// app.use('/v1', v1Router)  // v1Router just pins Api-Version = '2023-01-01'\n\ndeclare function getOrder(id: string): Promise<any>\ndeclare function applyVersionShims(o: any, v: ApiVersion): any"
      },
      {
        "lang": "typescript",
        "label": "Transform shim chain: downgrade the latest response to older versions",
        "code": "// Each shim knows how to convert a response from ONE version back to the\n// PREVIOUS one. The core always emits the newest schema; to serve an old\n// client we replay the response backward through the chain (Stripe pattern).\n\ntype Version = '2023-01-01' | '2024-06-20' | '2025-03-15'\nconst ORDER: Version[] = ['2023-01-01', '2024-06-20', '2025-03-15']\n\ninterface VersionChange {\n  // The version this change was INTRODUCED in.\n  version: Version\n  // Downgrade a response of `version` back to the prior version's shape.\n  downgrade(body: any): any\n}\n\nconst CHANGES: VersionChange[] = [\n  {\n    // 2024-06-20 split `name` into `first_name` + `last_name`.\n    // To serve pre-2024 clients, recombine into the old single field.\n    version: '2024-06-20',\n    downgrade: (b) => {\n      const { first_name, last_name, ...rest } = b.customer ?? {}\n      return { ...b, customer: { ...rest, name: [first_name, last_name].filter(Boolean).join(' ') } }\n    },\n  },\n  {\n    // 2025-03-15 renamed `amount` (cents) exposed as `amount_cents`.\n    // Older clients expect the field named `amount`.\n    version: '2025-03-15',\n    downgrade: (b) => {\n      const { amount_cents, ...rest } = b\n      return { ...rest, amount: amount_cents }\n    },\n  },\n]\n\n// Walk the latest response backward until it matches the client's version.\nexport function applyVersionShims(body: any, target: Version): any {\n  const targetIdx = ORDER.indexOf(target)\n  let out = body\n  // Apply downgrades for every change introduced AFTER the target version,\n  // newest-first, so the shape peels back one version at a time.\n  for (const change of [...CHANGES].reverse()) {\n    if (ORDER.indexOf(change.version) > targetIdx) {\n      out = change.downgrade(out)\n    }\n  }\n  return out\n}"
      }
    ]
  }
]
