import type { QuizData } from '@/types'

/** Backend Engineering quiz bank: concept, debug, and architecture questions. */
export const BACKEND_QUIZZES: QuizData = {
  concept: [
    { id: 'cq_1', topic: 'http-protocol', difficulty: 'beginner', q: 'What HTTP status code indicates a resource was successfully created?', options: ['200 OK', '201 Created', '204 No Content', '202 Accepted'], answer: 1, explanation: '201 Created is the correct status for successful resource creation. Include a Location header pointing to the new resource. 200 means an existing resource was found/returned.' },
    { id: 'cq_2', topic: 'http-protocol', difficulty: 'beginner', q: 'Which HTTP method is both safe (no side effects) AND idempotent (same result if called multiple times)?', options: ['POST', 'PUT', 'GET', 'PATCH'], answer: 2, explanation: 'GET is safe (no side effects) and idempotent (always returns same resource). PUT is idempotent but modifies state. POST is neither.' },
    { id: 'cq_3', topic: 'dns', difficulty: 'beginner', q: 'What DNS record type maps a domain to an IPv4 address?', options: ['CNAME', 'MX', 'A', 'TXT'], answer: 2, explanation: 'A record maps hostname → IPv4. AAAA maps → IPv6. CNAME aliases to another hostname. MX is for mail server routing.' },
    { id: 'cq_4', topic: 'middlewares', difficulty: 'mid', q: 'What happens if Express middleware calls next() after already calling res.json()?', options: ['Nothing — next() is a no-op if response is sent', 'The next middleware runs with the response already committed', 'Error: "Cannot set headers after they are sent"', 'The previous response is cancelled and re-sent'], answer: 2, explanation: 'Once res.json() sends headers, the response is committed. If next() leads to another handler trying to set headers, you get "Cannot set headers after they are sent". Always return after sending a response.' },
    { id: 'cq_5', topic: 'osi-model', difficulty: 'mid', q: 'A Layer 7 load balancer can route traffic based on which of the following?', options: ['IP address only', 'TCP port number only', 'HTTP headers, URL path, and cookies', 'Physical network interface MAC address'], answer: 2, explanation: 'Layer 7 (Application) LBs inspect HTTP content: URL path (/api vs /static), Host header, cookies (sticky sessions), User-Agent. Layer 4 LBs only see IP and port.' },
    { id: 'cq_6', topic: 'error-handling', difficulty: 'mid', q: 'Which status code is correct for "user is authenticated but does not have permission for this resource"?', options: ['401 Unauthorized', '403 Forbidden', '404 Not Found', '400 Bad Request'], answer: 1, explanation: '403 Forbidden = authenticated but not authorized. 401 = not authenticated (despite being named "Unauthorized"). This distinction matters for client-side handling.' },
    { id: 'cq_7', topic: 'tcp-ip-model', difficulty: 'senior', q: 'Why does HTTP/2 multiplexing NOT eliminate TCP head-of-line blocking?', options: ["HTTP/2 doesn't support multiplexing", 'HTTP/2 multiplexes at application layer but TCP delivers all data in order — one lost packet blocks all streams', 'HOL blocking only affects HTTP/1.1 and was fully solved in HTTP/2', 'HTTP/2 uses UDP internally'], answer: 1, explanation: 'HTTP/2 streams are multiplexed over ONE TCP connection. A single lost TCP packet causes the OS to wait for retransmission before delivering ANY subsequent data, stalling all HTTP/2 streams. HTTP/3 (QUIC/UDP) solves this with per-stream loss recovery.' },
    { id: 'cq_8', topic: 'authentication', difficulty: 'mid', q: 'Why should refresh tokens be stored in an HttpOnly cookie rather than localStorage?', options: ['HttpOnly cookies are faster than localStorage', 'HttpOnly cookies cannot be read by JavaScript — XSS attacks cannot steal them', 'HttpOnly cookies are automatically encrypted', 'Refresh tokens are too large for localStorage'], answer: 1, explanation: 'HttpOnly attribute prevents JS access to the cookie. XSS (injected JS) can steal localStorage tokens but cannot access HttpOnly cookies. This is the primary defense against token theft.' },
    { id: 'cq_9', topic: 'caching', difficulty: 'mid', q: 'What is cache stampede (thundering herd)?', options: ['Cache fills up and evicts all entries at once', 'Many requests hit the same cache key simultaneously when it expires, all miss and query the DB', 'Cache serves incorrect data to all users', 'Cache server crashes from too many writes'], answer: 1, explanation: 'When a popular cache key expires, all queued requests miss simultaneously and blast the database. Fix: probabilistic early expiration (randomly expire 5-10% early) or a distributed lock so only one request refreshes.' },
    { id: 'cq_10', topic: 'databases', difficulty: 'mid', q: 'What is the N+1 query problem?', options: ['Querying N tables requires N+1 JOINs', 'Fetching N records then running one additional query per record = N+1 total queries', 'Database connections pool N+1 connections unnecessarily', 'N rows require N+1 indexes to be efficient'], answer: 1, explanation: 'Classic pattern: fetch 20 users, then run 20 separate queries for each user\'s profile. Fix with a JOIN or batch load (WHERE id IN (id1, id2, ...)) to get everything in 1-2 queries.' }
  ],
  debug: [
    { id: 'dq_1', topic: 'http-protocol', difficulty: 'beginner', q: 'Your login API returns this. What is wrong?\n\nHTTP/1.1 200 OK\n{ "success": false, "error": "Invalid password" }', options: ['Nothing is wrong — 200 is standard for all responses', 'Should return 401 Unauthorized, not 200 OK with an error body', 'Should return 500 Internal Server Error', 'The JSON format is invalid'], answer: 1, explanation: 'Returning 200 for auth failures is incorrect. Clients check HTTP status codes for programmatic handling (retries, redirects to login). Auth failures must return 401. The body can contain details but status code must reflect the outcome.' },
    { id: 'dq_2', topic: 'middlewares', difficulty: 'mid', q: 'Every request to your API hangs. You added this recently:\n\napp.use((req, res, next) => {\n  if (isAuthenticated(req)) {\n    req.user = getUser(req);\n  }\n  res.json({ ok: true });\n});', options: ['isAuthenticated is async and not awaited', 'next() is never called — all requests terminate here with res.json({ ok: true })', 'res.json should be res.send', 'The middleware is registered before routes'], answer: 1, explanation: 'This middleware sends res.json() for EVERY request. Route handlers never run because the response is already sent. Fix: call next() instead (or only call res.json() for specific conditions).' },
    { id: 'dq_3', topic: 'dns', difficulty: 'beginner', q: 'You updated DNS A record to a new server IP. 4 hours later users still reach the old server. Most likely cause?', options: ['New server is misconfigured and rejecting connections', 'DNS TTL has not expired — resolvers cached the old record', 'The domain has expired', 'HTTPS certificate mismatch on new server'], answer: 1, explanation: 'DNS changes propagate as TTLs expire at each caching layer. If old record had TTL=86400 (24h), resolvers may cache it for up to 24 hours. Solution: lower TTL to 60-300s BEFORE changing servers, then restore after propagation confirms.' },
    { id: 'dq_4', topic: 'async-programming', difficulty: 'mid', q: 'This route returns undefined for user. Why?\n\napp.get("/users/:id", (req, res) => {\n  const user = db.findUser(req.params.id); // async\n  res.json({ user });\n});', options: ['db.findUser does not exist', 'Missing await — Promise is assigned to user instead of resolved value', 'req.params.id is undefined', 'res.json cannot serialize objects'], answer: 1, explanation: 'db.findUser() returns a Promise. Without await, user = Promise { pending }, not the resolved value. The handler must be async and await the call: const user = await db.findUser(req.params.id).' },
    { id: 'dq_5', topic: 'api-security', difficulty: 'mid', q: 'Users report they can see each other\'s orders. Your route:\n\napp.get("/orders/:id", requireAuth, async (req, res) => {\n  const order = await db.findOrder(req.params.id);\n  res.json(order);\n});', options: ['requireAuth middleware is broken', 'Missing ownership check — any authenticated user can access any order ID', 'Database returns wrong data', 'The route should use POST not GET'], answer: 1, explanation: 'Classic IDOR (Insecure Direct Object Reference). The route authenticates the user but does not verify they own the order. Fix: add if (order.userId !== req.user.userId) return res.status(403).json({ error: "Forbidden" }).' }
  ],
  architecture: [
    { id: 'aq_1', topic: 'client-server-architecture', difficulty: 'mid', q: 'A mobile app calculates the final order price (including discounts) on the client and sends it to the server for charging. What is the security problem?', expertAnswer: 'The client is untrusted. Any user can intercept the request (Charles Proxy, Burp Suite) and change the price to $0.01. Business-critical calculations — pricing, discounts, taxes — must always be computed server-side. The server should calculate the price from the cart items and ignore any price sent by the client.', keyPoints: ['Clients are untrusted by definition', 'Price calculation = business logic = server-side', 'This is a real e-commerce fraud vector', 'Server must be the source of truth for financial calculations'] },
    { id: 'aq_2', topic: 'error-handling', difficulty: 'senior', q: 'Your API returns 500 errors but your logs show nothing. A new engineer says "errors must be swallowed somewhere." How do you debug this systematically?', expertAnswer: '1) Check for unhandled async errors — missing try/catch in async handlers, or .catch() not attached to promises. 2) Verify error middleware is registered LAST (after all routes). 3) Check process.on("unhandledRejection") — if not set up, rejections fail silently in older Node. 4) Look for error thrown after res.send() (double response). 5) Add asyncWrapper to all async handlers to auto-catch and pass to next(err). Use process.on("uncaughtException") as last resort.', keyPoints: ['Async errors must be caught or passed to next(err)', 'Error middleware must be registered after all routes', 'process.on("unhandledRejection") catches everything else', 'asyncWrapper pattern prevents missing catches'] },
    { id: 'aq_3', topic: 'caching', difficulty: 'senior', q: 'You added Redis caching to your user profile endpoint and hit rate went to 95%. But users now see outdated profile pictures for up to 10 minutes. How do you fix this without reducing cache hit rate significantly?', expertAnswer: 'The TTL-based expiration causes stale data. Solutions in order of preference: 1) Cache invalidation on mutation — when profile is updated, immediately del the cache key. This is more reliable than TTL. 2) Shorter TTL for profile picture specifically (separate cache key). 3) Event-driven invalidation — pub/sub message on update triggers cache delete on all instances. The root fix is cache-aside invalidation on write, not just TTL expiration.', keyPoints: ['TTL is a fallback, not the primary invalidation strategy', 'Invalidate cache on mutation events', 'Separate cache keys per data type/freshness requirement', 'Event-driven invalidation for distributed systems'] }
  ]
}
